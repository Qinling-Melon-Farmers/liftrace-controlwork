
## 2026-09-19：显式接回高位导航研究基线
- 范围：baseline_files.json列出的uav_mission和planner文件；来源visionwork@778121a。原planner包快照已保存。
- 验证：导入文件逐字节与来源一致；本提交只接回既有研究代码，新增修复/编译随后独立提交。
- 遗留/下一步：按停滞与近墙设计实现有界恢复、诊断和安全复访，再集成验证。

## 2026-09-19：目标级停滞恢复、轨迹身份及近墙复访
- 改动：plan_manage新增可选4s/4cm无进展观察，运动状态与odom/地图新鲜度门控，同目标两次恢复后明确失败；保留deadline。Bspline携带源目标stamp/frame，拒绝旧generation/错目标解锁。双端10Hz与首次状态事件可选诊断。uav_mission新增近墙内侧观察点、低位新鲜候选准入、0.20m精细跟随。
- 验证：导航源码在独立catkin验证工作区编译成功，34项C++测试零失败；158项相关Python策略/事务回归通过。尝试全包发现后，依赖未复制的视觉/旧控制/模型夹具测试出现5失败20错误（301项），属于此隔离仓不具备整机夹具，保留记录；全包检查将在集成仓执行，不以选择性PASS覆盖这些结果。
- 范围：此提交无SITL，无实机部署；默认功能关闭，实验launch随后在视觉仓显式打开。55cm工程包络不重复膨胀。
- 遗留：LIO/FC高度分离另列调查，不改Gate阈值；单轮仿真不能证明所有历史失败布局修复。
- 下一步：按本提交精确同步visionwork研究分支，完成整机编译、参数/资源/身份检查后运行用户授权的一轮随机场景并录制overview。

## 2026-09-19：修正时间投影窗与弧长前视不一致
- 改动：实际seed36失败样条冻结单测；projectProgress改为受跟随距离约束的局部弧长搜索窗，两端同步；保留单调、无远端跳转与4096采样上限。修正旧集成测试对参数化manager节点的定位。
- 验证：实际C++样条回放旧实现FAIL（projection0、距终点1.369m），新实现5/5曲线测试PASS（32ms总测试耗时）；新窗口不强迫前进，只从实测位置选择可达局部分支。整机编译与全包回归在同步后进行。
- 遗留/下一步：仿真尚未运行；抽签seed2672已冻结，等待整机验证后执行唯一一轮。

## 2026-09-19：允许显式选择俯视录制相机模型
- 改动：水平场launch增加overview_model_sdf参数，原R64默认模型和5m视角不变；整机高位实验可选7m/近裁剪3.1m渲染模型以避开5.5m顶板。
- 验证：XML参数入口静态检查；只影响渲染相机，已有飞行代码与碰撞几何不改。seed2672飞行期间发现原7m相机空白，飞行本身不重跑。
- 下一步：视觉仓提供高位渲染模型并用本轮真值全程回放补录；视频必须标明回放，不冒充新飞行验证。

## 2026-09-19：用户订正后的机构预算与走廊分段优化输入
- 范围：NEXT_STAGE.md/yaml；不改已验收飞行参数。按用户先预留每投10s机构执行/确认，明确预算不是固定sleep，也不是已实施的执行超时。
- 几何：1.5m内墙，推荐FC上限1.2m、巡航0.9m，合成倾角≤15°；55cm包络不重复膨胀。姿态超出时需降低高度或暂停提速，不能独立roll/pitch各15°仍套此界。
- 验证：读取2025参考、机构驱动和当前事务/ACK链及seed2672逐段记录；无新仿真。暂不声称停滞彻底修复，保留初次规划反复与历史近墙回归。
- 下一步：整机仓实现连续门位、4m外围墙与双视角观察入口，3m只做离线几何；运行时异步机构完成契约另行实现。

## 2026-09-19：固定起飞坐标系与走廊分段调速
- 范围：uav_mission任务层、仿真布设入口；不改旧patrol_control或执行机构。
- 改动：按实时位置/固定墙平面切换0.40m门间前视与0.15m过门前视，0.75/0.95m滞回，入口下降及H前保守；现有最后投递至入口1m前视保留，不重置deadline。高位允许3m指令，粗网格bounds可配置。仿真spawner新增冻结靶位，执行同样的边界/墙/相互间距检查，真值只交布设节点。
- 接口：新增私有corridor_speed_schedule、high_view_full/grid与spawn/frozen_layout参数，沿用全部既有话题和服务；研究launch启用，默认路径不变。视觉线索高度区间由视觉仓配置。整机场景采用新X=旧Y、新Y=-旧X，固定起飞系，机体/传感器相对外参不旋转。
- 验证：完整集成工作树318项任务测试通过（含调速4项、冻结布局2项），高位库70项、轻量场景79项通过；完整launch展开核对坐标、限高、网格、真值隔离和五靶足迹。导航单仓全量测试的旧控制/模型/ROS依赖缺失不能当整机回归结果，已在完整集成仓补验。
- 遗留：这条记录时SITL尚未开始。10s机构预算仍是待标定预算，不把mock服务ACK说成实物掉落反馈，不插入10s固定睡眠。未部署硬件。
- 下一步：新地图同seed三版完整SITL、双机位录像、逐阶段节时分析；保留失败，禁止无分析重复。

## 2026-09-19：冲突高位线索先低空逐点复核，补搜边界减速
- 触发：新起飞系A开发轮panzer先出现于pillbox附近（距中心1.6cm、距真正panzer1.58m），3.750s后记忆发生冲突；原逻辑整类隔离后两投进入遍历补搜，在边界换段初规划连续失败并出现航段超时。历史旧朝向同seed未出现该冲突。缺当时原始机载帧，不能断言旋转敏感性是唯一根因。
- 用户要求：保留两处坐标低空复核，而非立即重新遍历。修复后重跑原速/走廊优化/3m搜索三轮；截图标明XY与初始机头。
- 改动：视觉NavigationMemory保留每类最多两处冲突假设，仍从合格高位线索中隔离；新增verification_hints只供导航复核，不产生释放许可。epoch重置清空、TTL不刷新、数量有界。导航先用地图距离/空间距离选复核点，失败/超时换另一处，确认后走原新鲜低位APPROACH/释放链，已投类别不再复核；两点均耗尽才补搜。单点运动≤25s、重捕≤15s；开始复核75s后不再发新复核段（最后重捕最多额外15s），原600s总deadline不重置。
- 补搜：只在靠边0.85m范围或距边缘目标最后1m使用0.20m前视，含RESUME；中心段仍1m。研究启动参数把首次规划等待上限设为12s，避免无轨迹时每次消耗90s；不伪报成功，不解除障碍柱/接触约束。
- 验证：323项完整任务测试、70项高位库通过；新增两假设隔离/过期/epoch、错类不释放、换点成功、不可达换点与迟到结果拒绝、两处耗尽后兜底、近墙末段减速测试。此前整机构建已通过，本补丁为Python/参数；本记录时三轮重跑尚未开始。
- 开发轮：frame2672_A_baseline_20260919_200425在确认首个补搜超时后主动停止，缺完整Gate，不能列PASS或完赛时间。两路视频有可解码容器；原始全部保留；统一包装器最终cleanup PASS，复查零残留（手动停止与包装器并发复查曾短暂看到rosout，最终已消失）。
- 交付：frame_axes.png/SVG标明固定起飞系新X=旧Y、新Y=-旧X，初始机头+X。图为实际观察相机截图加坐标标注，不是误分类时机载原图。未部署机载、未合并main。
- 下一步：同一修订代码、同一冻结新地图重跑A/B/C，录制与阶段图表，旧开发轮单列。

## 2026-09-19：搜索内环硬约束、H独立高度窗口与新相机参数接线
- 用户指定同seed2672再跑A/B/C，修复与视场优先；不新增时间优化代码。
- 规划SDF加入可关闭的已知搜索域有符号距离/占据约束，机心按墙内界另留0.4m；仅进入正式投后路线时关闭。高位支持树柱的跟踪余量可配置，低位恢复原值，保持完整柱体，不开启越树顶捷径。
- 独立Gate对SEARCH/EXECUTING检查55cm整机投影是否在墙内搜索域；H最后接近/识别在限定区域采用独立高度上限，不影响其他走廊区。相机光学四元数和机架路径沿launch完整透传。
- 任务LAND前设置仿真专用相位参数，供视觉仓Gazebo守护盒按用户允许切换50cm；接触监视器保留地面/H支持事件、采样力/法向/深度，障碍接触仍单独统计。
- 验证：导航独立plan_env构建通过，C++测试结果8（含gtest容器汇总）零失败；42项相关Python测试通过（内环、H限高、支持/墙同时接触分离、既有冲突复核/速度回归）。整机集成构建及新三轮实跑待后续。
- 旧规划源码快照与清单/SHA256：legacy_baseline/20260919_inner_region/。仅研究feature，未改硬件、未推main。

## 2026-09-19：补齐独立真值Gate回归的姿态消息
- 全量329项任务回归暴露旧测试桩缺少真实ModelStates必有的orientation；补齐单位四元数，保留原有高度漂移断言，不改飞行代码。
- 下一步：整机全量回归及已授权三轮。

## 2026-09-20：FSM显式保持、起飞首层与全阶段首轨迹修复
- 用户要求严肃修复FSM和多次起飞失败，并明确不另开分支；直接在feat/high-view-liveness-20260919继续。没有改main、膨胀、水平柱、碰撞边界或释放门槛。
- traj_server进展补goal_seq；FSM按goal_seq/traj_id/traj_start三重身份订阅SERVER tracking_hold，0.25s去抖后重规划。显式保持与4s/4cm物理停滞共享同一目标级两次预算，耗尽后任务桥终端失败并取消，不再落入0.20–0.45m阈值死区或继续等动作总期限。
- A*首层在当前加速度外增加经原速度/占据/20ms碰撞检查的目标方向原语，并记录起终点占据、同体素、碰撞、速度、闭集、剪枝、接纳和超时计数；初次/重试分开输出，未建立穿障例外。
- 高位Probe增加可选低空staging：低空进入场内后同点升高，失败不得跳过；低位观测不写入高位记忆，缺目标时沿该已验证升降柱返回。默认空配置兼容旧入口，集成研究launch另行显式启用。
- 新execution/initial_plan_timeout覆盖所有规划动作从未产生有效轨迹的情况；旧search_initial_plan_timeout保持兼容。解决seed31 RETURN_HOME仍等90s的监督缺口。
- 验证：77项定向Python通过；导航C++依赖闭包真实编译通过；plan_manage/path_searching等65项C++/rostest零失败，新增起步原语、占据起点诊断、SERVER身份/去抖/新鲜度和共享预算测试。全工作区原始构建的driver2 ROS2/ament与导航仓缺视觉包问题用依赖闭包隔离，没有改系统环境。
- 遗留：本提交未启动SITL/实机，不能宣称seed31/32/34动态根治。下一步精确同步现有visionwork研究分支，完成整机编译/全量回归和launch展开；动态定向回归需新的明确仿真授权。

## 2026-09-21：复查FSM修复并核对两轮新实飞资料
- 修正SERVER监控误拒ROS目标序号0；保持通知在新鲜度短暂中断时保留，直到消费或新有效轨迹/目标取代。
- 幂等任务重放补首轨迹超时检查；A*节点time/time_idx及time_origin初始化，消除静态搜索未定义值。同步现有导航、研究、板端分支，不新开分支，不部署硬件。
- 39项执行桥回归通过，C++编译/监控用例在整机工作树验证。两项外部FrozenR54测试缺点云时不能算真实回放通过，旧65项是Catkin容器汇总。
- 新实飞23点最后目标有READY和持续非零命令，接管前不是已证明的tracking_hold；现场源码仍旧时间投影窗且缺新恢复，地图存在门区结构。22点路线不同，不能当同布局对照。报告位于板端docs/deployment/flight_review_20260921/REPORT.md。
- 遗留：真实机体净空、飞控响应及局部投影需同步ULog/进展消息区分；起飞SITL尚未动态验收。本轮只读bag和修复/离线验证，未启动飞行。


### 2026-09-21：昨晚提交复核、A*夹具修正与seed32动态验收
- 范围：导航搜索测试、研究验收报告及板端试飞分析。
- 改动：补齐绕过initMap的测试夹具search_region开关/边界初始化，新增空体素但人工区域外起点拒绝用例；三份导航文件同步。复盘13点后7个提交，记录模型入口修复与本次整场结果。
- 验证：A*重编译后8项通过，外部FrozenR54两项排除；结合其他74项共82项有效测试通过。seed32完成起飞及两投，第三点复访起点Y越过-4.4人工区域，12秒首轨迹期限结束，整场FAIL；未进入走廊和降落。包装器及精确进程检查零残留。
- 遗留：投递许可与规划合法区域不一致；SERVER保持恢复本轮未触发，seed34未跑。夹具初始化修复不代表历史32/34运行时根因。
- 下一步：统一边界目标投后恢复交接的几何契约，再做有针对性的仿真，不重复原样跑失败轮。


### 2026-09-21：共享机体包络消除视觉与规划人工区域冲突
- 范围：plan_env搜索区域、研究fov_inner_repair入口与报告；导航权威修改同步两份集成。
- 改动：可选引用现有BoundaryRevisit场地/55cm包络/偏航配置推导规划硬中心区，视觉3cm跟踪余量与规划2.5cm净空启动校验。仅研究入口启用；真实点云膨胀未改，无新节点/状态机/重试预算，板端未部署。
- 验证：fast_planner_node与traj_server编译成功；4项C++边界与4项Python视觉边界测试通过；seed32离线launch展开及历史交接点几何验证通过。边界测试补齐缺失main后实际执行。
- 遗留：几何验证不保证真实点云净空；SERVER恢复动态覆盖及完整seed32回归仍待进行。本轮未启动仿真。
- 下一步：明确授权后针对seed32投后第三点、走廊与降落验收；不再叠加通用看门狗。


### 2026-09-21：延期复核、统一APPROACH许可及优化设计
- 范围：uav_mission的mission_core/high_view_full及测试；研究分支另含速度/2025槽位/旋转覆盖文档。
- 改动：复核超时或复访失败先利用其他线索，保留原每类两次复访预算，最后再重访延期目标；分配槽位前统一APPROACH几何许可，LOW_COVERAGE及已入队候选不能绕过。原释放、恢复与任务deadline未放宽；无新增FSM/计时器。
- 验证：研究整机uav_mission完整340项通过，含定向28项/新增6项；板端专项8项通过；三份代码一致。本轮只改Python逻辑，无C++变更，未仿真、未操作硬件。
- 文档：完赛优化仅分析；2025参考包含7cm与10cm不同补偿及软件ACK非物理脱离证据；牛耕名义覆盖100%但非实际保障，十套走廊模板XY旋转核对通过。
- 遗留：实际恢复位置可规划性、近边误差、34树附近停滞仍需独立验证；单元回归不等价于seed40整场通过。旧槽位补偿未直接启用，速度参数未改。
- 下一步：明确授权后做对应回归；优化/机构补偿先按设计审阅，不自动上板或续跑。

### 2026-09-23：高位漏扫片区优先补搜与近墙局部复核
- 范围：uav_mission/high_view_full.py及对应单元测试；导航权威现有feature分支。
- 改动：记录高位不可达原始航点，把对应完整低空航带排在常规补搜前；同片区多个跳点共用航带。三条高位线索齐备而仅剩近墙目标时，先安排边界内两点、有总时限的低位视觉复核，仍由统一边界许可和原投递链决定是否进入对准与释放。
- 验证：针对航带优先、重复片区、近墙复核与耗尽路径的测试通过；研究工作树整机任务344项单元测试通过，导航与研究两份源码一致。
- 遗留：三组既有SITL运行使用修订前源码；本次导航修订没有新的动态飞行验收，不宣称seed38/40已通过。seed34的三维下降初轨迹失败另需处置。
- 下一步：经明确授权后单独回归被跳过的高位航带与近墙目标，不将研究配置直接部署机载。
