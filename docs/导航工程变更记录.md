# 导航工程变更记录

## 2026-08-30｜VCL06 导航消息合同与比赛 profile 冻结

- 日期：2026-08-30
- 分支：`feat/vcl06-contract-profile`
- 改动范围：`uav_mission` 消息/配置/测试、导航合同说明、导航组原始交接资产。
- 具体改动：
  - 新增 `NavigationDecision` 与 `NavigationResult` v1，固定命令、状态、阶段、目标身份、
    幂等关联及 payload 不可逆提交字段。
  - 新增 `competition_profiles.yaml`：正式 `r2026` 仅包含
    `tent=1.0、pillbox=1.5、bridge=2.0、panzer=2.5、red_cross=10.0`，明确排除
    `tank`，且不在任何 competition profile 中记录它。
  - 接通 Catkin 消息生成/运行依赖，修正 `catkin_python_setup()` 调用顺序，移除不存在的
    `include/` 导出，并令 C++ 占位节点依赖本包消息生成目标。
  - 增加静态、确定性的 profile/消息合同测试；导航组提供的两份原始资产按原字节归档。
- 验证结果：
  - `/usr/bin/python3 .../test_contract_profile.py`：5/5 PASS。
  - `catkin_make -DROS_EDITION=ROS1 -DCATKIN_WHITELIST_PACKAGES=uav_mission -j1`：PASS，
    C++/Python/EusLisp/Lisp/Node.js 六类消息产物及占位 C++ 节点均生成成功。
  - `catkin_make ... run_tests_uav_mission -j1` 与 `catkin_test_results --verbose`：
    5 tests，0 errors，0 failures，0 skipped。
  - 两份归档资产分别与视觉集成根目录原件执行 `cmp`：均一致。
- 遗留问题：现有 manager 尚未发布/消费新消息，也尚无持久队列、结果 reducer、planner
  结果回流或三槽调度；正式 `r2026` 收到 tank/未知 profile 的运行时拒绝将在任务核心 PR
  实现。
- 下一步：基于本合同实现无 ROS `MissionCore`，以
  `(mission_id, target_id, target_first_seen)` 管理候选生命周期，并补陈旧/重复/乱序结果回归。

## 2026-08-30｜VCL06 decision 执行租约补强

- 日期：2026-08-30
- 分支：`feat/vcl06-contract-profile`
- 改动范围：`NavigationDecision` 合同、静态测试与合同说明。
- 具体改动：新增 ROS 绝对时间 `deadline`，令 manager 与 execution bridge 共享同一决策
  租约；明确过期后不得进入新阶段，但真实 guarded release ACK 无论传输是否延迟都必须
  回报，防止“物理已投却因超时复用槽位”。
- 验证结果：合同静态测试 5/5 PASS；`uav_mission` 白名单串行构建 PASS；Catkin
  5 tests，0 errors、0 failures、0 skipped；未启动 ROS master、仿真或硬件链路。
- 遗留问题：现有旧 manager/executor 尚未消费该字段，由后续运行桥实现。
- 下一步：运行桥把 `CoreAction.deadline_at` 无损映射到本字段并做过期决策回归。

## 2026-08-30｜VCL06 确定性任务核心

- 日期：2026-08-30
- 分支：`feat/vcl06-mission-core`
- 改动范围：`uav_mission` profile 加载、候选队列、任务 reducer、确定性测试和设计说明。
- 具体改动：
  - 新增无 ROS `CompetitionProfile` 加载器，未知 profile、空类别、非法权重和非法 top-k
    启动失败；正式 `r2026` 的中断集合固定为 `red_cross、panzer、bridge`。
  - 新增严格候选准入、稳定实例键、按类别去重的持久优先队列、顺序三槽、重试冷却及
    已投类别去重。
  - 新增仅限 SEARCH 的高权重中断、覆盖结束/动态时限低权重兜底、510 s 硬返航和
    三次提交即返航策略。
  - 新增结果身份核对、executor/event sequence 幂等 reducer，以及
    `release_ack_success` 后槽位不可回滚和恢复失败返航语义。
  - 补齐全局 decision sequence、冻结派发快照、targetless motion result、目标/搜索/返航/
    降落 deadline 和返航失败 ABORT；`APPROACH` 明确为带阶段回报的复合投递事务。
  - 新增 `CoverageRoute`：中断不推进、恢复原 nominal waypoint、匹配成功才推进、重复/
    错序结果无副作用，连续失败按上限显式跳过。
  - 将比赛配置、profile 权重和派发快照冻结；目标事务未见提交 ACK 就超时时隔离载荷槽位、
    禁止重试并返航，延迟且身份匹配的释放 ACK 仍可收敛为已提交，避免误复用已释放载荷。
  - decision 使用排他边界的有限执行租约，result 使用显式事件时间；超期释放 ACK 的归约
    同时受事件时间与 manager 接收时间约束，不依赖 timer/result 回调顺序；隔离事务保留
    release tombstone，510 s、600 s、4 m 硬边界均在构造和运行期 fail-closed。
  - 增加释放阶段安全锁存：未提交失败仅在 RELEASE 前释放槽位；一旦观察到 RELEASE/
    RECOVERY，后续阶段回退也不能解除隔离，迟到 guarded ACK 可把 tombstone 收敛为已提交。
- 验证结果：
  - `PYTHONPATH=src /usr/bin/python3 -m unittest discover -s test
    -p 'test_*.py' -v`：56/56 PASS。
  - `catkin_make -DROS_EDITION=ROS1 -DCATKIN_WHITELIST_PACKAGES=uav_mission -j1`：PASS。
  - `catkin_make ... run_tests_uav_mission -j1` 与 `catkin_test_results --verbose`：
    56 tests，0 errors，0 failures，0 skipped。
  - 未启动 ROS master、Gazebo、PX4、舵机或实机动作。
- 遗留问题：纯核心尚未消费 ROS 消息；planner status 和 decision/result
  运行话题仍需独立 manager 桥接，统一仿真实跑仍待视觉进程释放资源后排队执行。
- 下一步：新增只发布 `/navigation/mission_command_raw` 的 ROS manager，消费候选和
  `/navigation/mission_result`；正式模式不直接争用 `/fastplanner/goal`。

## 2026-08-31｜VCL06 导航任务运行编排

- 日期：2026-08-31
- 分支：`feat/vcl06-manager-runtime`
- 改动范围：`uav_mission` 无 ROS 运行协调器、ROS1 manager 壳、launch/yaml、测试与
  运行说明。
- 具体改动：
  - 新增 `MissionRuntime`，以同一 `RLock` 原子编排 `MissionCore + CoverageRoute`；覆盖
    搜索中断后恢复原 waypoint，成功推进、有限失败跳过，陈旧/错序结果不移动游标。
  - 固定 `r2026` 三槽任务策略：仅 `red_cross、panzer、bridge` 中断 SEARCH，低权重等待
    覆盖结束或时间兜底，三次提交立即返航，510 s 硬返航、600 s 上限、4 m 高度边界。
  - 搜索 lease 普通超时才累计 waypoint 失败；硬返航无论由 timer 还是晚到 result 触发，
    均以 `route.interrupt` 退休当前搜索，不推进 cursor、不标记 skipped。
  - 增加单调时钟门与幂等 fail-closed：ROS 时钟回退使用最后有效时刻只生成一次 ABORT；
    `executor_changed` 在运行核心事务内原子 ABORT；目标超时隔离后仍接受身份匹配的晚到
    guarded release ACK，且不覆盖正在执行的 RETURN。
  - 新增手动启动的 `navigation_mission_manager.py`，只发布 latched
    `/navigation/mission_command_raw` 与诊断状态，不发布 planner goal、不触碰 Servo、PWM、
    解锁或飞控。候选、result、pose、map、timer 和服务回调由外层锁串行化。
  - 正式 profile 强制新鲜、同 frame、非空且数据布局完整的地图 readiness；回调异常被
    兜底并 fail-closed，避免 Noetic timer 线程退出；补齐 `std_srvs` 与 `python3-yaml`
    运行依赖。
- 验证结果：
  - `PYTHONPATH=src python3 -m unittest discover -s test -p test_*.py`：77/77 PASS。
  - source ROS Noetic 与视觉 overlay 后，`uav_mission` Catkin 白名单串行构建 PASS；
    `catkin_test_results`：77 tests，0 errors，0 failures，0 skipped。
  - `python3 -m py_compile`：运行协调器与 ROS manager PASS；生成后的
    `uav_mission.msg` Python 包导入 PASS。
  - `roslaunch --dump-params` 与 `roslaunch --files`：PASS，展开值确认
    `r2026 / 510 s / 600 s / 4 m / search z=2.2 m / require_map=true`。
  - 两轮独立只读复核最终均未发现 P0/P1；`git diff --check` PASS。
  - 未启动 ROS master、Gazebo、PX4、执行机构或任何实机动作。
- 遗留问题：尚无 execution bridge 将 raw decision fenced 地转换为 `/fastplanner/goal` 并
  消费 planner telemetry；默认 pose 接线必须在联调前确认实际输出为 `camera_init`，有字节
  但全 NaN 的点云仍需运行态语义诊断；统一 SITL 闭环尚未执行。
- 下一步：在独立分支实现 execution bridge，严格按 `decision_seq + deadline` 接入 planner
  goal/status 与 `NavigationResult`，继续保持投递机构和实机动作隔离。

## 2026-08-31｜VCL06 规划执行安全桥

- 日期：2026-08-31
- 分支：`feat/vcl06-execution-bridge`
- 改动范围：`uav_mission` 无 ROS 规划执行核心、ROS1 adapter、launch/yaml、规划遥测依赖、
  测试与运行说明。
- 具体改动：
  - 新增 `PlannerMotionExecutor`，以整数纳秒归约连续 decision、排他 deadline、planner
    生命周期和 odom 到达证据；冷启动、重复/冲突序号、planner event 回退、接受超时、
    planning attempt、替换目标 CANCEL 栅栏和未来/陈旧数据均 fail-closed。
  - `decision_seq` 原样进入 planner goal `header.seq`；planner 的 requested/effective goal
    必须完整回证，effective goal 限于同 frame、0–4 m、相对请求目标不超过 1.10 m；
    `TRAJECTORY_FINISHED` 后仍需新鲜同帧 odom 满足 3D 距离、速度和连续驻留。
  - 固定 `camera_init + r2026 + 三槽 + 五类`，正式类仅
    `tent/pillbox/bridge/panzer/red_cross`；与视觉 stable ID 对齐，允许目标 ID 为 0，并由
    `has_target` 区分无目标。
  - 新增 `navigation_planner_bridge.py`：完整验证 ROS 消息后先发布隔离 planner goal，再发布
    同一 executor 的 `ACCEPTED/DISPATCH`；结果身份与事件序号冻结。APPROACH 到达只产生
    `PROGRESS/PLANNER` 和 target-transaction intent，不伪造 payload commit。
  - 默认 `execution_enabled=false`，默认目标话题为 `/navigation/fastplanner_goal`；真实
    `/fastplanner/goal` 在代码中硬阻断且不创建 publisher。CANCEL、target transaction、LAND、
    HOLD、ABORT 全部仅诊断，不连接 replan、Servo、PWM、MAVROS 或执行机构。
  - 修正 `plan_manage` 虚假导出不存在同名库的问题，使下游可声明消息依赖；同步 planner
    `trigger_` 显式初始化修正。
  - 增加 planner/core 跨合同回归，证明 goal 发布后的非终态 dispatch ACK 与 2 s planner
    accept timeout 均能被 MissionCore 正确归约。
- 验证结果：
  - `PYTHONPATH=src/uav_mission/src python3 -m unittest discover ...`：118/118 PASS。
  - `plan_manage + uav_mission` Catkin 白名单串行构建 PASS；当前运行的 planner gtest
    4/4、`uav_mission` 118/118 PASS，均无 error/failure。
  - `python3 -m py_compile`：规划核心与 ROS adapter PASS；`git diff --check` PASS。
  - `roslaunch --dump-params/--files`：PASS；展开确认 execution/live/kino/manual 四项默认
    均为 false，`camera_init / r2026 / 4 m / 三槽 / 五类无 tank`。
  - 完成导航—视觉只读字段审查；候选、frame、时间/TF 与 stable key 主链对齐，未修改视觉
    工作树。未启动 ROS master、Gazebo、PX4、执行机构或实机动作。
- 遗留问题：Fast-Planner 尚无带 ACK 的 cancel/hold 与 traj_server stop；末端
  `ReleaseEvidence` 缺冻结实例/观测时间，drop aligner 未消费导航冻结目标；单一 target
  transaction coordinator、landing executor、mission reset epoch 与统一 SITL Gate 仍待实现。
- 下一步：先由同一 executor coordinator 补齐冻结目标 CAPTURE/ALIGNMENT/RELEASE/RECOVERY
  合同，同时设计 planner cancel + traj_server hold/stop ACK；两者完成前继续禁止 live goal。

## 2026-08-31｜VCL06 规划运动桥职责净化

- 日期：2026-08-31
- 分支：`feat/vcl06-execution-bridge-review`
- 改动范围：`PlannerMotionExecutor`、ROS bridge、launch/yaml、合同说明与纯测试。
- 具体改动：
  - 删除冷启动必须 SEARCH/seq1、decision 序号严格连续和无界 decision/planner event 历史；
    晚启动可接收仍在 deadline 内的当前 decision，同 mission 旧序号作为 stale 忽略。
  - 未知旧 goal telemetry 与 event 回退不再永久 fault；当前 goal 的同序号内容冲突和生命周期
    冲突仍 fail-closed。bridge 仅保留当前 decision、当前/待取消 goal 与最近 planner event。
  - 删除 bridge 对 profile、准入类别和载荷槽位的重复策略校验，继续保留 goal frame、高度、
    deadline、planner lifecycle、effective goal、odom 距离/速度/dwell 等运动执行约束。
  - live 目标发布改为双显式开关：默认仍关闭；`execution/enabled=true` 且 live 话题下
    `allow_live_goal_output=true` 时允许创建并发布 `/fastplanner/goal`。
- 验证结果：相关纯 Python 单测与合同测试 PASS；未启动 ROS master、仿真或实机动作。
- 遗留问题：本桥仍只负责 planner motion；target transaction、landing、通用 hold/stop ACK
  不在本分支实现，不能据此宣称完整可飞闭环。
- 下一步：与 manager 组合验证 replacement 的旧 goal `CANCELLED` 先于新 goal `ACCEPTED`，
  再由独立分支实现目标事务协调器。

## 2026-08-31｜VCL06 规划运动桥第二轮瘦身

- 日期：2026-08-31
- 分支：`feat/vcl06-execution-bridge-review`
- 改动范围：规划运动 pure core、ROS wrapper、聚焦测试与合同说明。
- 具体改动：
  - 删除通用 `MotionIntent` 分发层，core 只返回可发布 planner decision、typed result 与明确
    handoff；ROS wrapper 不再重复解释一套 intent 类型。
  - 删除进程永久 fault 状态；当前 active 身份或生命周期冲突只生成当前 action 的 FAILED，
    未知/陈旧输入继续忽略，后续 manager 新 decision 可恢复执行。
  - LAND/HOLD/ABORT 与 APPROACH 后续目标事务仅报告 handoff，不模拟外部执行。
  - 将规划执行测试从内部防御分支枚举收敛为晚启动、幂等、替换 cancel fence、telemetry
    隔离、deadline、odom dwell、APPROACH 交接及外部命令边界等关键合同。
- 验证结果：`uav_mission` 全量纯测试 95/95 PASS；py_compile、launch XML、
  `git diff --check` PASS。未启动 ROS master、仿真或实机动作。
- 遗留问题：pure core 与 ROS 消息转换仍保留必要的结构校验；目标事务、landing 与通用
  hold/stop ACK 继续由后续独立执行器负责。
- 下一步：组合 manager 与 PlannerStatus 做无仿真的 replacement 时序集成测试。

## 2026-08-31｜VCL06 规划桥参数与事件合同修正

- 日期：2026-08-31
- 分支：`feat/vcl06-execution-bridge-review`
- 改动范围：规划 goal/frame 合同、ROS callback 恢复策略、planner typed event 与测试。
- 具体改动：
  - mission frame 不再固定为 `camera_init`，只要求非空并以 launch/yaml 参数为权威。
  - goal 接受有限非零四元数，在 pure core 规范化后贯穿 requested/effective goal 并保留发布，
    删除 wrapper 对 identity 四元数的逐位重复校验。
  - malformed 单消息改为告警后忽略，不永久禁用输出；内部发布身份不一致等 RuntimeError 仍
    停止输出。
  - planner `ACCEPTED` 新增 `STARTED/PLANNER`，`TRAJECTORY_READY` 和
    `FAILED_ATTEMPT` 新增非终态 `PROGRESS/PLANNER`；不在 tick 中重复发布事件。
- 验证结果：`uav_mission` 全量纯测试 97/97 PASS；未启动仿真或实机动作。
- 遗留问题：实际联调所用 odom frame 仍须与配置一致；外部执行 handoff 不在本桥实现。
- 下一步：在组合分支用非 identity 姿态、非默认 frame 与 malformed 后恢复序列做 ROS 级测试。

## 2026-08-31｜PR #6 规划运动生命周期收口

- 日期：2026-08-31
- 分支：`feat/vcl06-execution-bridge`
- PR：`sakelier/liftrace-controlwork#6`
- 改动范围：VCL06 planner motion lifecycle、ROS bridge、合同说明与 PR 级验证收口。
- 具体改动：
  - 将执行桥职责限定为单一 planner motion 生命周期：接收有效且未过期的
    `NavigationDecision`，保持 `decision_seq → planner goal → PlannerStatus` 关联，并以
    planner 生命周期和同 frame odom 距离、速度、连续驻留共同判定到达。
  - planner `ACCEPTED` 输出 `STARTED/PLANNER`，`TRAJECTORY_READY` 与
    `FAILED_ATTEMPT` 输出非终态 `PROGRESS/PLANNER`；APPROACH 到达只交接
    `TARGET_TRANSACTION`，不伪造投递提交或目标终态。
  - mission frame 以参数为权威；有限非零目标四元数经规范化后贯穿请求、有效目标与发布；
    malformed 单消息告警后忽略，内部发布身份不一致仍停止输出。
  - planner goal 输出保持双显式开关且默认关闭；本 PR 不实现 target transaction、LAND、
    Servo/PWM、planner hold/stop ACK，也不宣称完整可飞闭环。
  - 修复全 PR diff 中两处历史 EOF 多余空行，不改变运行参数或 launch 行为。
- 验证结果：
  - `uav_mission` 全量纯 Python 回归：97/97 PASS。
  - `plan_manage + uav_mission` Catkin 白名单串行构建 PASS；`uav_mission` Catkin 测试
    97/97 PASS，planner schema/lifecycle gtest 4/4 PASS。
  - `python3 -m py_compile`、生成消息与 Python 模块导入、两个导航 launch 的
    `roslaunch --files/--dump-params` 均 PASS。
  - `git diff --check origin/main...HEAD` PASS；验证未启动 ROS master、Gazebo、PX4、MAVROS、
    执行机构或实机动作。
- 验收边界：
  - 仓内 `route_revision` 只是导航默认配置标签；正式 VCL06 的 `toudi3-random` 场景、路线与
    对应参数由集成层 launch/yaml 显式覆盖，不在本 PR 内改写导航仓默认世界资产。
  - 整机 SITL Gate、随机场可达性、地图完整性与 planner map 波动属于后续集成验收，不作为
    本 PR 的 motion-lifecycle 合同与构建测试验收项。
- 遗留问题：target transaction、视觉冻结上下文、guarded release、LAND executor 与带 ACK
  的 planner hold/stop 继续由独立后续工作交付；本 PR 不扩大到这些职责。
- 下一步：按依赖顺序合入前置 PR 后整理 stacked diff，再由集成层执行 ROS 组合与 SITL Gate。

## 2026-09-02｜VCL06 视觉上下文消息合同同步

- 日期：2026-09-02
- 分支：`feat/vcl06-local-full-mission`
- 基线：PR #6 头 `d95377c`
- 改动范围：仅同步导航正式任务链硬依赖的两条 `uav_vision` ROS 消息合同及消息生成注册。
- 具体改动：
  - 增加 `AlignmentTargetContext.msg` 与 `ReleaseEvidenceContext.msg`，供导航目标事务冻结
    stable ID、任务/decision 身份、对准模式、截止时间及几何关联结果；
  - 只修改 `uav_vision/CMakeLists.txt` 的 `add_message_files`，不迁移检测、评测、板端或
    视觉运行实现；消息内容来自视觉集成分支 `34ce0c3`；
  - 保持原 `ReleaseEvidence.msg` 不变，避免破坏旧消息 MD5 与既有消费者。
- 验证结果：消息定义与 CMake 注册完成；将在后续正式导航链提交后一并执行 Catkin 构建、
  Python 消息导入和导航单测。
- 遗留问题：运行时仍需使用视觉组已冻结的发布链提供对应上下文话题；本提交只提供单仓
  构建所需接口，不代表视觉实现随导航仓复制。
- 下一步：移植 PR6 后继的正式 manager/start gate/目标事务/释放与 LAND 链并完成整包验证。

## 2026-09-02｜VCL06 本地完整任务链同步到导航 fork

- 日期：2026-09-02
- 分支：`feat/vcl06-local-full-mission`
- 基线：导航 PR #6 头 `d95377c`
- 来源：视觉集成冻结点 `liftrace-visionwork:feat/vcl06-local-full-mission@34ce0c3`
- 改动范围：`uav_mission` 正式任务/执行链、`patrol_control` 外部任务与 H 降落、
  Fast-Planner/FAST_LIO/FreeDOM 仿真接线、随机场资产、依赖元数据、测试与 HANDOFF。
- 具体改动：
  - 按最终文件白名单移植起飞完成 start gate、覆盖路线、前三权重中断、同 stable ID 目标事务、
    释放许可/mock ACK、三投后 11 点路线、三门 Gate、H 新鲜度对齐与失败关闭 AUTO.LAND；
  - 修正 PlannerStatus 序列合同、规划桥目标生命周期、仿真点云解析/时间戳和 20×20×5 m 内部
    仿真地图参数，并保留实机规划参数不变；
  - profile 固定 `r2026`，比赛队列为 `tent/pillbox/bridge/panzer/red_cross`，不含 tank；
    `interrupt_top_k=3` 使 red_cross、panzer、bridge 均可中断搜索；
  - 未迁入旧 `coverage_search_manager.py`。将随机场生成器唯一需要的类别 profile 校验收进
    `random_field_policy.py`，删除旧 `coverage_policy.py` 及 YAML 中无人读取的
    `interrupt.min_weight` 等策略段；
  - 导航仓只同步两条 `uav_vision` 上下文消息用于单仓生成接口，不复制检测、板端或视觉运行
    实现；完整入口必须叠加视觉冻结分支并让该 `uav_vision` overlay 优先；
  - 按旧控制源码修改要求，在 `legacy_baseline/20260902_nav_pr7/` 保存 PR #6 基线下完整
    `patrol_control` 快照、27 项文件清单和 SHA256，校验 27/27 PASS；
  - 补齐 `nav_msgs`、Gazebo、Fast-LIO、plan_manage、rosbag 与测试依赖，注册此前未执行的
    `test_new_vision_config.py`。
- 验证结果：
  - 导航仓 `uav_vision` 消息包构建 PASS，两条新增上下文消息可生成；
  - `plan_manage;patrol_control;uav_mission` Catkin 串行构建 PASS；
  - Catkin 汇总 `219 tests, 0 errors, 0 failures, 0 skipped`，其中 planner gtest 4 项、
    patrol_control gtest 7 项、patrol_control nose 11 项、uav_mission 186 项；
  - `uav_mission` 纯 Python 回归 186/186 PASS，新增配置回归 9/9 PASS，py_compile 与
    `git diff --check` PASS；
  - 使用视觉 `34ce0c3` overlay 时，完整 launch 的 `roslaunch --files`、
    `roslaunch --dump-params` 和 ROS 消息导入 PASS；未启动 ROS master、Gazebo、PX4、舵机或
    任何实机运动节点。
- 遗留问题：本次是仓库拆分后的代码/合同同步，没有重跑完整 SITL。r9 的
  `planner_accept_timeout` / `planner_requested_goal_mismatch`、实际高度 `4.002849 m` 和仅完成
  一投仍是当前运行态阻塞；第二/第三投、三门走廊与 H/LAND 尚无完整 Gate PASS。
- 下一步：将本分支推送到团队导航 fork，保持 PR #6 不变；待 PR #6 合入后再整理后继 PR，
  同时与视觉冻结分支组成双仓 overlay 继续分段及完整仿真。

## 2026-09-02｜同步六项已合入 PR 并准备后继 PR

- 日期：2026-09-02
- 分支：`feat/vcl06-local-full-mission`
- 上游基线：`origin/main@a182ca9`
- 改动范围：将导航仓已合入的 PR #1～#6 主干结果同步回后继 feature 分支，准备向上游提交 PR。
- 具体改动：
  - 确认上游 `main` 已包含 PR #1、#2、#3、#4、#5、#6，且 PR #6 头 `d95377c` 是主干祖先；
  - 以 `--no-ff` 合入最新主干，补入后于 PR #6 合并的 FreeDOM 静态点云可视化降采样、配置、
    测试和相关 FAST_LIO/RViz 接线；
  - `FreeDOM/src/visualization.cpp` 自动合并无冲突，同时保留 VCL06 所需的静态点云 ROS 时间戳和
    上游可视化改动；不改写既有后继提交历史。
- 验证结果：
  - `freedom;plan_manage;patrol_control;uav_mission` Catkin 串行构建 PASS；
  - Catkin 汇总 `237 tests, 0 errors, 0 failures, 0 skipped`；
  - 活跃源码/配置/文档 `git diff --check` PASS；未启动 ROS master、Gazebo、PX4、执行机构或实机。
- 遗留问题：本次只消除与最新主干的集成差异，不改变 r9 运行态阻塞结论；完整双仓 SITL Gate
  仍需在视觉冻结 overlay 下复跑。
- 下一步：推送更新后的 fork 分支，以该分支为 head、上游 `main` 为 base 创建后继 PR，并在 PR
  中明确 121 文件含 legacy 快照以及当前未通过完整 Gate 的边界。

## 2026-09-02｜创建后继 PR 并复核导航整仓

- 日期：2026-09-02
- 分支：`feat/vcl06-local-full-mission`
- PR：`https://github.com/sakelier/liftrace-controlwork/pull/8`（Open，非草稿）
- Issue：`https://github.com/sakelier/liftrace-controlwork/issues/9`（Open）
- 改动范围：更新导航 HANDOFF、任务包 README 与合同状态；复核上游、fork、工作树、包清单和
  全工作区构建边界。
- 具体改动：
  - 以团队 fork 同名分支为 head、导航上游 `main` 为 base 创建 PR #8；创建时 head 为
    `5c97107`、base 为 `a182ca9`，没有直接推送/合并 `main` 或打 tag；
  - 把 PR #1～#6 已全部合入、后继 PR #8 已建立、双仓 overlay 边界和 r9 未通过事实同步到
    HANDOFF/合同/README；
  - 盘点主导航工作区：Catkin 全量遍历 29 个包，仓库无 `.github/workflows`；本地 `main` 与
    fork `main` 仍停在 `a68925d`，后继 feature 已完整包含上游 `main@a182ca9`；
  - 补跑全量 Catkin 配置，确认仓内 `tool/cv_bridge/src` 缺少 `CMakeLists.txt`，且
    `local_sensing` 仍依赖当前环境不存在的 `cmake_modules`。因此此前四包定向 PASS 不扩写成
    全工作区 PASS；两项可复现阻塞已集中提交为上游 Issue #9。
- 验证结果：PR API 返回 HTTP 201、`state=open`、`draft=false`；feature 相对上游主干为
  behind 0 / ahead 3（文档提交前）；既有四包定向构建和 237 项测试仍为 PASS。全工作区 Catkin
  配置为 FAIL，两个首要配置阻塞如上；未启动 ROS master、Gazebo、PX4、MAVROS、执行机构或实机。
- 遗留问题：P0 仍是 planner 目标接受/取消/替换代际竞态和 4 m 高度余量；随后是第二/第三投、
  三门走廊、H/LAND 完整 Gate。仓级另缺全量构建修复、自动 CI 和导航/视觉完整联合回归。
- 下一步：先在 PR #8 分支修复 planner 代际与轨迹高度并跑“首投恢复→第二目标接近”分段 Gate；
  业务链稳定后再处理全工作区构建卫生并执行完整双仓 SITL。

## 2026-09-02｜登记导航轻量策略仿真仓

- 日期：2026-09-02
- 分支：`feat/vcl06-local-full-mission`
- 外部仓：`https://github.com/sakelier/liftrace-sim.git`，记录时 `main@18f6ee8`
- 改动范围：导航完整任务 HANDOFF 与 `uav_mission` README。
- 具体改动：
  - 登记导航组新增的纯 Python 任务级仿真仓，明确其用于完整 SITL 前快速设计、批量扫描和比较搜索/
    投递策略；
  - 记录其现有 M0～M2-D 范围：牛耕航迹、视场/Cue Monte Carlo、V-SIM-04 查表、障碍与 A*、
    遮挡、中断投递恢复、600 秒计分、有限载荷策略和动态阈值扫描；
  - 固定职责边界：该仓不是 ROS/Catkin 工作区，不加入导航—视觉运行 overlay，不直接下发飞控参数；
    其候选结论须人工评审后进入 `liftrace-controlwork` feature，并继续通过分段/完整 SITL Gate；
  - 保留模型限制：避障计分、三门、自主降落、真实动力学/LIO/planner 波动和完整视觉
    `P_interrupt` 尚未闭合，当前结果不能作为整场得分预测或验收证据。
- 验证结果：只读核对远端默认分支、提交、README、配置、模型脚本和测试清单；没有修改或推送
  `liftrace-sim`，没有运行 ROS、Gazebo、PX4、执行机构或实机动作。
- 遗留问题：尚未建立轻量仓参数到导航 YAML 的带版本导出合同，也未把候选策略与现有 r9 场景做
  配对复现；这些工作应在 P0 planner 代际与高度阻塞修复后进行。
- 下一步：完整任务 Gate 恢复后，从轻量仓选取少量有清晰证据标签的候选参数，建立“模型候选→
  导航 feature→SITL 实证”的单向对照表，避免两仓参数静默漂移。

## 2026-09-04｜切换为导航上游直接协作分支

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 上游分支：`sakelier/liftrace-controlwork:feat/vcl06-local-full-mission`
- 被替代 PR：`https://github.com/sakelier/liftrace-controlwork/pull/8`
- 改动范围：远程协作拓扑、HANDOFF、合同与 `uav_mission` README。
- 具体改动：
  - 仓库 owner 授权直接编辑导航仓并建议先拉上游新分支，以避免继续审阅一次性包含 121 个文件的
    大 PR；
  - 核对上游 `main` 仍为 `a182ca9`，当前完整任务分支包含该主干且无落后，上游不存在同名分支；
  - 写权限 dry-run 通过后，在原始仓库创建 `feat/vcl06-local-full-mission`，创建时 tip 为
    `b328ef2`，并把本地分支的 upstream 从团队 fork 切换到 `origin`；
  - 团队 fork 同名分支保留为历史备份，不再是当前工作入口；原 PR #8 关闭为 superseded，后续先
    在上游 feature 修复业务 Gate，再按消息合同、任务执行、旧控制补丁和仿真资产拆分小 PR；
  - 没有推送或合并 `main`，没有打 tag，也没有修改视觉仓或轻量仿真仓。
- 验证结果：`git push --set-upstream origin` 成功创建上游 feature；本地分支开始跟踪
  `origin/feat/vcl06-local-full-mission`。本轮仅调整协作方式和文档，未启动 ROS、Gazebo、PX4、
  MAVROS、执行机构或实机动作。
- 遗留问题：分支仍保留 r9 的 planner 目标接受/替换竞态、4 m 高度余量、第二/第三投、三门与
  H/LAND 未通过；上游 feature 的存在不等于运行 Gate 或合入条件已满足。
- 下一步：以 Issue #9 或业务 P0 为边界建立小提交/小 PR，优先修复 planner 代际和轨迹高度，得到
  分段 Gate 后再决定完整任务集成的最终拆分方式。

## 2026-09-04｜修复规划目标代际竞态与最终指令超高

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 改动范围：VCL06 planner bridge/executor、旧控制外部任务限高、合同测试与接口文档。
- 具体改动：
  - 将 planner 接收窗口由 2 秒调整为 5 秒，覆盖 Fast-Planner 单线程重规划回调在复杂地图负载下
    的实测阻塞；
  - 不再把会被 ROS 发布层改写的 `PoseStamped.header.seq` 当作任务代际，而是用 planner 原样回传
    的决策 `issued_at` 时间戳映射回 `decision_seq`；未知迟到代际仅忽略，不再误归约成当前目标；
  - 取消栅栏只约束已收到 planner `ACCEPTED` 且未结束的旧目标；从未接收便超时的旧目标不等待
    不可能出现的取消 ACK，避免替换目标死锁；
  - 在 `patrol_control` 距离插值完成后再次执行 3.5 m 最终指令限高，堵住“当前飞机已高于上限时，
    插值把预先限高的目标重新抬高”的路径；任务目标上限继续保持 2.5 m。
- 验证结果：Python 语法检查和 45 项直接单元/合同测试 PASS；相关 Catkin 定向构建 PASS；
  `catkin_test_results build/test_results` 汇总 `239 tests, 0 errors, 0 failures, 0 skipped`；
  `git diff --check` PASS。未启动实机、执行机构或任何硬件节点。
- 遗留问题：上述两项 P0 已完成源码与回归级修复，但仍需在冻结视觉 overlay 上复跑 seed 11，确认
  高度全程低于 4 m，且 r9 的第二目标阶段不再出现 `planner_accept_timeout`、
  `planner_requested_goal_mismatch` 或路线跳点。
- 下一步：按统一 `sim_run.sh` 运行 seed 11 完整 SITL Gate，记录 bag/Gate 报告并更新 HANDOFF。

## 2026-09-04｜r10 实跑确认 P0 并修正末端交接合同

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：`/home/xhj/liftrace/logs/vcl06_p0_seed11_r10_20260904_023743/`
- 改动范围：APPROACH 到达阈值、ROS Header 业务身份边界、VCL06 Gate 与合同测试。
- 具体改动：
  - r10 的 11 个 planner 目标均正确接收，r9 的 `planner_accept_timeout`、
    `planner_requested_goal_mismatch` 和路线跳点未复现；高度峰值为 `3.242780 m`，零碰撞、零越界、
    零超高，确认前一提交两处 P0 的实跑效果；
  - r10 完成 panzer、bridge 两次完整投递，第三个 red_cross 的 planner 轨迹结束时机体距 effective
    goal 为 `0.340220 m`，而原 0.30 m 阈值导致其持续等待并在 90 秒 action deadline 终止；为不放宽
    搜索、返航和走廊航点，仅给随后必经视觉精对齐的 APPROACH 增加有界 `0.35 m` 交接阈值；
  - 实跑期间新增只读 `rostopic` 订阅使 rospy 顶层传输序号递增，暴露 `Header.seq` 不能作为
    `NavigationDecision/NavigationResult` 业务身份；bridge/Gate 改用显式 `decision_seq/event_seq`，旧
    `MissionCommand` Gate 关联改用不会被顶层 publisher 重写的嵌套 `goal.header.seq`；
  - 本轮不修改消息定义，不引入第二个执行器或新分支，也不放宽 4 m 高度、碰撞、边界、载荷提交和
    post-delivery 路线 Gate。
- 验证结果：直接 Python 单元/合同测试 `21 + 16 + 33` 项 PASS；Catkin
  `run_tests_uav_mission` PASS；全结果汇总 `241 tests, 0 errors, 0 failures, 0 skipped`；
  `git diff --check` PASS。r10 结构化 Gate 为 FAIL，原因是修补前的第三目标 deadline/传输 Header
  误判，不能记为整场 PASS。
- 遗留问题：0.35 m APPROACH 交接和 Header 修正仍需 r11 复跑；三投后的 11 点走廊、H 对齐和
  LAND 尚未被本轮触达。
- 下一步：在同一 seed、同一视觉冻结 overlay 上运行 r11，优先确认三投完成并继续进入
  post-delivery 路线；若仍失败，按首次新阻塞更新 HANDOFF，不回退已经实证的两处 P0 修复。

## 2026-09-04｜r11 实跑关闭两处 P0 并移交走廊近目标阻塞

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 导航代码：`919e438`
- 视觉运行 overlay：`feat/vcl06-local-full-mission@34ce0c3`
- 运行证据：`/home/xhj/liftrace/logs/vcl06_p0_seed11_r11_20260904_030218/`
- 改动范围：只更新导航完整任务 HANDOFF 与联调记录；本条不新增运行代码。
- 具体改动：
  - 用与 r10 相同的正式 seed 11 随机场、PX4/Gazebo SITL、冻结视觉 overlay 和 mock Servo 复跑
    完整链；未连接实机、真实飞控或真实投递机构；
  - 确认三个前三权重目标依次完成：panzer/slot 1、bridge/slot 2、red_cross/slot 3，raw mock ACK、
    release commit、recovery 均为 3/3，`visual_delivery_audit.json` 为 PASS；
  - 确认 r9 的规划代际链式故障未复现，所有已派发目标均正确归属，没有
    `planner_accept_timeout`、`planner_requested_goal_mismatch`、未知结果或路线跳点；
  - 确认高度修复生效：Gate 最高实测高度 `3.147812 m`，高度/碰撞/边界违规均为 0；
  - 记录新的首次阻塞：三投后 route index 2 已进入 Wall_15 门后，距 Fast-Planner 调整后的
    effective goal 从 `0.159 m` 降至约 `0.13 m`；此时重规划失败并在 `Close goal` 条件下高频重试，
    最终 `planner_attempt_limit_exceeded`，Mission Manager 以 `return_home_failed` 中止；
  - 更新 HANDOFF，把两处原 P0 标记为已关闭，把近目标终态、后续两门和 H/LAND 标为剩余功能缺口。
- 验证结果：
  - 结构化 Gate 为 FAIL，原因 `manager_failed`，不得宣称整场通过；
  - Gate 指标：`release_commit_count=3`、`recovery_success_count=3`、`height_violations=0`、
    `actual_collision_count=0`、`boundary_violations=0`；只记录到 Wall_15 一次有效过门；
  - r10/r11 联合证据足以关闭用户指定的两处 P0：规划目标接收/替换竞态和最终指令超高；
  - 运行结束后 `sim_run.sh` 已回收 roscore、roslaunch、gzserver、PX4，无残留仿真进程。
- 遗留问题：完整 Gate 尚未 PASS。需修复 Fast-Planner 近目标重规划失败的终态处理，并继续验证
  Wall_20、Wall_22、H 新鲜度对齐、AUTO.LAND、落地与解锁；实机执行机构仍未验收。
- 下一步：在同一 feature 分支补一个有界的近目标终态修复及回归，避免增加并行分支；随后复跑
  seed 11，首次完整 `gate_status.json=PASS` 后再拆分小 PR 供 owner 审阅。

## 2026-09-04｜修复近目标终态与决策源时钟抖动

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_latestvision_oldcamera_r14_20260904_163107/`
- 视觉运行 overlay：`feat/vsim04-ks2a543-camera-baseline@6743099`；本轮仍使用旧
  `640x480/60 deg` Gazebo 相机几何以隔离导航改动。
- 改动范围：planner motion executor 的近目标收口、源时钟容差及确定性回归。
- 具体改动：
  - 仅当同一决策已出现过有效 `TRAJECTORY_READY`，才允许在 planner 因近目标重规划而不再发布
    `TRAJECTORY_FINISHED` 时，由新鲜同 frame 里程计继续执行距离、速度和连续驻留判定；没有有效轨迹、
    超距、超速、陈旧里程计或驻留中断仍不能成功；
  - 正常 `PLANNING/REPLANNING` 遥测不再无条件清空合格驻留，但每个新里程计样本仍按最新 effective
    goal 重新校验；失败次数超限也只在合格驻留仍新鲜时短暂延后，决策 deadline 保持不变；
  - r14 的第三个 fallback APPROACH 在发布端与接收端 `/clock` 相差约 `1 ms` 时被旧代码误判为
    `decision_from_future`，导致第三目标根本没有下发；现把既有 `100 ms` 源时钟容差一致用于决策入口，
    超过容差的真正未来决策仍失败关闭；
  - 新增“有效轨迹后近目标驻留成功”“驻留期间失败预算延后”“无有效轨迹仍失败”和“决策时钟容差”
    回归，未扩大到达距离、速度、目标偏移、碰撞、高度或任务时限。
- 验证结果：直接执行器单测 `25` 项 PASS；`catkin_make run_tests_uav_mission -j1` 后汇总
  `245 tests, 0 errors, 0 failures, 0 skipped`；`git diff --check` PASS。r14 为正式 FAIL，已完成
  panzer、red_cross 两次投递，最高高度 `3.188920 m`、零碰撞、零越界、零超高；第三个 pillbox 决策
  因上述 1 ms 抖动未被 Bridge 接受，故该轮没有验证近目标补丁和走廊/H/LAND，不能记为整场通过。
- 遗留问题：需在相同 seed 和旧相机几何下复跑确认第三投与 r11 近目标阻断关闭；随后切换
  KS2A543 `1280x720/82.85 deg` 相机模型和最新视觉投影链执行正式整场 Gate。
- 下一步：先做旧相机隔离复跑；只有得到走廊/H/LAND 完整终态后，才把新相机视场与 2.4 m 搜索高度
  纳入同一整场对照，避免把导航终态与相机变化混为一个变量。

## 2026-09-04｜对齐整场 Gate 的源时钟容差

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_latestvision_oldcamera_r15_20260904_165935/`
- 视觉运行 overlay：`feat/vsim04-ks2a543-camera-baseline@6743099`；仍使用旧
  `640x480/60 deg` Gazebo 相机几何进行单变量隔离。
- 改动范围：只调整只读整场 Gate 对 MissionCommand 时间戳的绑定容差及其回归，不修改任务状态机、
  规划器、投递条件、碰撞/高度/边界阈值或任务时限。
- 具体改动：
  - r15 中 Bridge 已正常接受 panzer APPROACH，但只读 Gate 仍要求 command stamp 不早于
    decision issued；两个节点消费同一仿真 `/clock` 时出现约 `1 ms` 的正常采样先后差，Gate 因
    `mission_command_approach_stamp_out_of_range` 提前结束；
  - Gate 现复用运行配置中的 `readiness/stamp_future_tolerance`，默认 `50 ms`，只放宽
    MissionCommand 的下界绑定；deadline 上界保持严格，目标 ID、类别、任务代际和业务序号仍须匹配；
  - 参数只接受 `0..1 s` 有限值；新增 `40 ms` 抖动可绑定和 `51 ms` 超限拒绝的边界回归。
- 验证结果：断言模块单测 `34` 项 PASS；`catkin_make run_tests_uav_mission -j1` 汇总
  `246 tests, 0 errors, 0 failures, 0 skipped`；`git diff --check` PASS。r15 本身仍是 FAIL，只证明
  修复前 Gate 的零容差误报，不能作为任务链完成证据；最高高度 `2.257738 m`、零碰撞、零越界、零超高。
- 遗留问题：需在旧相机几何下重跑完整三投、走廊、H 对齐和 LAND；通过后再切换 KS2A543 新相机。
- 下一步：提交并推送该最小 Gate 修正，然后执行旧相机 r16 隔离复跑。

## 2026-09-04｜按连续门洞轨迹修正三门走廊

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_latestvision_oldcamera_r16_20260904_170548/`
- 视觉运行 overlay：`feat/vsim04-ks2a543-camera-baseline@6743099`；r16 是最后一轮旧
  `640x480/60 deg` 相机隔离跑，后续按用户要求直接切换 KS2A543。
- 改动范围：三门走廊航点、Fast-Planner 门洞参数、有效目标偏移上限及确定性回归。
- 具体改动：
  - r16 三投在约 `321 s` 全部完成，前两个走廊目标也已成功，证明近目标终态和 Gate 源时钟修正
    生效；Gate 随后在 Wall_15 报 `door_lateral_out_of_bounds`，全程仍为零碰撞、零越界、零超高；
  - bag 显示 Wall_15 门心目标被 0.1 m 体素膨胀地图向墙南侧调整 `0.70 m`，执行器误把该替代点
    当成到达；下一个北侧目标遂绕墙，实际在 `x=-0.949 m` 穿越墙平面，而门洞范围为
    `[-2.787,-1.987] m`，因此 Gate 属于有效功能失败而非误报；
  - 参照 2025 旧工程“门前/门后”连续穿越方式，删除三扇门平面上的停靠点，用成对航点直接跨越
    开口；Wall_15 高度改为开口竖向中心附近的 `0.90 m`，路线版本升级为
    `toudi3-random-three-door-h-r2`，共 `8` 个后处理航点；
  - 规划膨胀和优化净距由 VCL06 额外加严值恢复为 `0.20 m`，与 0.8/0.9 m 门洞及 0.1 m 体素
    分辨率匹配；Gazebo 0.30 m 半径接触环与碰撞 Gate 继续判定真实擦碰；
  - planner effective goal 最大偏移从 `1.10 m` 收紧到 `0.35 m`，避免大幅退回墙侧的替代目标
    被误报成功；搜索和前三个投递目标在 r16 的实测最大偏移均为 `0`，门前/门后调整为
    `0.291/0.300 m`，仍在该界内。
- 验证结果：任务管理器合同 `8` 项、Planner Bridge 合同 `16` 项、整场 Gate reducer `34` 项
  全部 PASS；`git diff --check` PASS。按用户指示不再复跑旧相机，运行验证直接进入新相机新视觉
  无 GUI 整场 Gate。
- 遗留问题：新路线、KS2A543 整机相机、三门、H 对齐和 LAND 尚待同一轮完整实跑；r16 仍是
  正式 FAIL，不得当成整场通过证据。
- 下一步：接入 KS2A543 `1280x720/82.85 deg` 内参与畸变的完整机架，执行无 GUI 整场 Gate；
  PASS 后再执行 GUI + RViz 可视化复验。

## 2026-09-04｜接入 KS2A543 整机相机与可视化订阅

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 视觉基线：`feat/vsim04-ks2a543-camera-baseline@6743099`。
- 改动范围：Gazebo 完整机架、正式整场 launch 的相机/搜索参数、RViz 订阅及合同测试。
- 具体改动：
  - 新增完整 `iris + MID360 + contact guard + KS2A543` SDF；保持既有机身外参
    `xyz=(0,0,-0.05)` 和 ROS 光学 frame，只替换为视觉组实测基线的 `1280x720`、
    `82.85 deg` HFOV、`fx=725.351 px`、主点和 plumb-bob 畸变参数；
  - 正式 `navigation_search_delivery_vcl06.launch` 默认使用该新 SDF，仍保持
    `/downward_camera/image_raw`、`/downward_camera/camera_info` 和
    `downward_camera_optical_frame` 接口不变；
  - 按视觉 HANDOFF 建议把正式搜索高度覆盖为 `2.4 m`，规划最大速度继续为 `1.0 m/s`；普通
    manager launch 默认仍为 `2.2 m`，只在正式 KS2A543 整场入口显式覆盖；
  - 现有 `patrol_pc.rviz` 新增 KS2A543 原图、交叉/H debug、圆环 debug 三个 Image 订阅，并保留
    静态地图、膨胀地图、规划轨迹、planner goal 和 MAVROS setpoint；GUI 复验时传
    `rviz:=true enable_debug_image:=true` 即可由单一 RViz 实例观察。
- 验证结果：SDF 与 launch XML 语法检查 PASS，正式 launch 展开节点清单 PASS；新增相机内参、话题、
  搜索高度和 RViz 合同回归后，`uav_mission` 编译通过，包内 `197 tests, 0 errors, 0 failures,
  0 skipped`，`git diff --check` PASS。
- 遗留问题：SDF 的实际 CameraInfo、视觉投影、三投、三门、H 对齐和 LAND 必须由接下来的新相机
  无 GUI 整场 Gate 验证；当前只完成静态合同和构建，尚不能宣称整场 PASS。
- 下一步：提交并同步现有 feature 分支，启动 KS2A543 + 最新视觉 overlay 的无 GUI seed 11 整场。

## 2026-09-04｜按新相机实跑放宽完整目标事务预算

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_ks2a543_headless_r17_20260904_173638/`
- 视觉运行 overlay：`feat/vsim04-ks2a543-camera-baseline@6743099`。
- 改动范围：只调整正式随机场完整目标事务时限，并冻结相应配置合同；不修改视觉新鲜度、投递互锁、
  碰撞/高度/边界 Gate 或全场 600 秒上限。
- 具体改动：
  - r17 已实测 Gazebo 发布 `1280x720`、`fx=fy=725.351 px`、`cx=631.672 px`、
    `cy=397.566 px` 和视觉组标定畸变，确认运行的确是 KS2A543 新相机基线；
  - 第一目标 panzer 已完成视觉证据、slot 1 mock ACK、release commit 和搜索恢复；第二目标
    red_cross 在 ROS `227.305 s` 已形成连续 5 帧有效释放证据，证明新相机主点、精修、投影和
    对准链能闭合；
  - 第二目标决策在 `143.200 s` 发出，旧 `90 s` 截止为 `233.200 s`；复杂地图接近和低空对准
    用尽窗口，机体抵达 `0.25 m` 释放高度时 Manager 已先以
    `target_action_timed_out_uncertain` 撤销事务，距离真实可投仅约 1 秒；
  - 正式随机场 `target_action_timeout` 因此由 `90 s` 调整为 `120 s`。该窗口只约束单个
    APPROACH/ALIGN/释放事务，任务仍在 `420 s` 强制返航、`600 s` 全场和 `180 s` 返航降落预留内；
  - 通用 VCL06 配置仍保持 `90 s`，避免把随机场实测调度余量扩散到其他场景。
- 验证结果：r17 结构化 Gate 为 FAIL，唯一顶层错误是 `manager_failed`，失败点为上述第二投事务
  超时；当轮最高高度、碰撞和边界违规均为 0。配置调整后的确定性合同与新相机无 GUI整场仍待复跑。
- 遗留问题：需用同一 seed 11 直接复跑新相机无 GUI Gate，验证三投、修正后的三门连续轨迹、H 对齐
  和 LAND；只有该轮 PASS 后才启动 GUI + RViz 复验。
- 下一步：运行配置/任务单测后提交并推送同一 feature 分支，执行 KS2A543 headless r18。

## 2026-09-04｜停止重复整场并定位新相机恢复确认阻塞

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_ks2a543_headless_r18_20260904_180128/`
- 改动范围：记录 r18 终态和 r11 对照结论，停止继续整场复跑；本条不再调整运行阈值。
- 具体改动：
  - r18 的 panzer 决策在 ROS `26.004 s` 发出，`39.949 s` 进入 ALIGN，`44.280 s` 获得严格视觉
    上下文，`71.083 s` 完成 slot 1 mock ACK 和 release commit；因此新相机下的识别、对准、许可与
    投递链已经闭合，失败不是“无法投递”；
  - 旧控制随后在 `75.943 s` 同时发布 `Run_point(1)` 和 `align_mode=disabled`，记录位姿约在
    `75.5 s` 已升过 `0.95 m`，后续长期位于约 `1.1..1.3 m` 且由位姿差分观察为低速；但 Bridge
    未发布 `RECOVERY SUCCEEDED`，直到 `146.000 s` 决策截止后 Manager 以
    `committed_recovery_timed_out` 强制返航；
  - r11 同类首投在 `71.053 s` commit、`75.803 s` 切回运行态、`76.454 s` 即完成 recovery，随后
    三投 3/3；当前 `navigation_planner_bridge.py` 相对 r11 记录的 `919e438` 没有代码差异，故不是
    候选队列、投递槽或后续导航补丁造成的确定性倒退；
  - 可观测的恢复条件（投递后运行态、关闭对准、恢复高度、位姿低速）均已满足。现有 debug bag
    没有记录 Bridge 实际消费的 `/mavros/local_position/odom.twist`，Bridge 状态也不输出逐项拒绝原因；
    剩余边界收敛到 odom frame/stamp/twist 或状态收据瞬时新鲜度，属于恢复 ACK 判定的潜在抖动，
    不能再靠盲目延长目标事务时限掩盖；
  - `target_action_timeout=120 s` 仍保留：它针对 r17 红十字在旧 90 秒边界刚形成有效证据的问题；
    r18 首个 panzer 的释放和物理恢复本可在原 90 秒内完成，继续把 120 秒加大不会修复本轮阻塞。
- 验证结果：r18 结构化 Gate 为 FAIL，`release_commit_count=1`、`recovery_success_count=0`，错误
  `manager_failed`，具体原因为 `committed_recovery_timed_out`；最高高度 `2.025776 m`，碰撞、边界、
  超高均为 0。按用户要求不再启动 r19 或 GUI 运行。
- 遗留问题：下一轮仿真前应先让 Bridge 输出恢复各谓词状态，并把瞬时 raw twist 的逐样本硬复位改为
  有界时间窗位姿运动判定，或以新证据证明真正不稳定；修复需保留 release ACK 后状态栅栏、恢复高度
  和连续驻留，不得把 commit 直接等同 recovery。
- 下一步：以本条和更新后的 HANDOFF 交接，不再消耗整场仿真；后续先做可确定回归的 recovery
  判定修复，再安排单次新相机 headless 验证。

## 2026-09-04｜收敛恢复与降落终态确认

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 问题证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_ks2a543_headless_r18_20260904_180128/`
- 改动范围：Planner Bridge 的恢复/降落终态确认、共同位置驻留组件、诊断录包和确定性回归；不修改
  搜索/选靶/权重、视觉证据、payload slot、投递代理、活动规划到达、碰撞/高度/边界 Gate 或任务时限。
- 具体改动：
  - 删除 recovery 和 landing 对单帧 `Odometry.twist` 的瞬时速度硬门槛，改用共同的
    `PositionSettleWindow`：恢复默认在三维 `0.15 m` 半径内连续驻留 `0.5 s`，降落默认在三维
    `0.15 m` 半径内连续驻留 `1.0 s`；超半径或 odom 间断会重启窗口；
  - 保留 release ACK / LAND command 后的控制态与对准态代际栅栏、高度、落点半径以及 MAVROS
    `ON_GROUND` 等必要事实；删除状态话题还必须每 `0.5 s` 重复刷新的冗余心跳约束；
  - 将 ABORT、LAND、RECOVERY、LANDING 四处 odom frame/stamp 新鲜度判断收敛为共同函数，并与
    PlannerMotionExecutor 一致地接受配置内 `source_future_tolerance`；
  - Bridge 状态新增 recovery/landing 驻留原因、时长和位移；正式 debug bag 补录其实际消费的
    `/mavros/local_position/odom` 和 `/detect/point_class`，避免再次出现只能猜测 twist、时间戳或
    控制态的不可诊断失败；
  - 相邻审查确认活动规划到达仍保留速度门槛，因为它用于防止高速掠过航点，和终态恢复/落地的
    物理驻留语义不同；严格释放证据与硬 Gate 未作放宽。
- 验证结果：
  - r18 PoseStamped 离线回放从 `75.943 s` 的投递后运行态开始，新窗口在 `76.459 s` 达标，窗口
    位移 `0.0410 m`，与 r11 的 `76.454 s` recovery 实证一致；
  - Python 语法、launch XML、配置/Bridge 合同和位置驻留单测 PASS；
  - `uav_mission` 定向构建 PASS；`catkin_make run_tests_uav_mission -j1` 汇总
    `203 tests, 0 errors, 0 failures, 0 skipped`；`git diff --check` PASS。
- 遗留问题：本条未启动 r19，故 r17/r18 仍是正式 FAIL；KS2A543 下的三投、r2 三门走廊、H 对齐和
  LAND 仍需下一轮单次 headless 完整 Gate 才能验收。
- 下一步：提交并推送同一 feature 分支；后续协调视觉组只跑一轮 KS2A543 headless，PASS 后再跑
  一次 GUI + RViz，不恢复无休止仿真。

## 2026-09-04｜拆分走廊至降落阶段验收

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 问题证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_ks2a543_headless_r19b_20260904_185602/`
- 改动范围：增加投递后路线的显式分段启动方式和 scope-aware Gate；不修改正式整场默认入口、
  payload 槽位/证据、视觉算法、规划参数、舵机或实机路径。
- 具体改动：
  - r19b 在 KS2A543 新相机基线上完成 panzer、red_cross 两投，分别于 ROS `77.538 s`、
    `206.507 s` 结束恢复；第三个 bridge 于 `228.803 s` 发起，直至 `348.803 s` 截止仍未进入
    `0.35 m` 规划接管半径；
  - bag 显示第三段 Fast-Planner 连续 129 次重规划均给出 `TRAJECTORY_READY`，但目标距离在
    `0.435..1.975 m` 间往返，实际高度在 `1.603..3.140 m` 间周期性升降。因此它不是目标丢失或
    规划无解，而是特定初始状态/目标组合触发的连续重规划极限环；
  - 修正耗时口径：r11 在 `223.985 s` 结束的是第二投，三投完成并进入 post-delivery route 的时间为
    `285.920 s`；r16 对应时间为 `309.078 s`。不能再把“220 余秒”写成三投总用时；
  - `MissionCore`/`MissionRuntime` 新增显式 `post_delivery` 验收启动方法，从 INIT 直接发布版本化
    走廊首段，但三个 payload slot 保持 FREE、commit 数保持 0，不伪造三投证据；
  - Manager 通过 `runtime/start_mode` 选择 `full` 或 `post_delivery` 并在状态中公开；正式入口默认
    仍为 `full`；新增 `navigation_post_delivery_vcl06.launch` 将分段启动与
    `gate_scope=post_delivery` 成对锁定；
  - scope-aware Gate 在分段模式只检查唯一路径发布者、路线顺序/目标、三门几何、H 新鲜证据、
    landing align、ON_GROUND、最终 disarm、时限/高度/边界/碰撞；任何 APPROACH 或非零提交槽均失败，
    且不会生成“三投完成”的假 PASS。
- 验证结果：Python 语法、launch XML、`git diff --check` 和递归 launch 解析 PASS；任务核心、Manager、
  Gate 定向 64 项回归 PASS；`uav_mission` 定向构建 PASS，包内汇总
  `209 tests, 0 errors, 0 failures, 0 skipped`。
- 遗留问题：分段 headless 尚未实跑；r19b 暴露的 Fast-Planner 重规划极限环尚未修改，需通过后段
  独立运行判断其是否同样影响走廊低空段，再对初速度连续性/近终点重规划策略做定向修复。
- 下一步：提交并推送当前分段能力；仅运行一次 `navigation_post_delivery_vcl06.launch` headless Gate，
  以首个失败阶段作为后续修复边界，不恢复整场盲目复跑。

## 2026-09-04｜锁存已确认 H 锚点避免降落高度往返

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r20_20260904_194209/`
- 改动范围：只修正外部 LAND 事务在 H 已稳定确认后的状态保持；不修改 H 检测/投影、首次对齐
  新鲜度、锚点范围、稳定帧数、AUTO.LAND 显式仿真开关、走廊路线或实机路径。
- 具体改动：
  - r20 的 8 个投递后航点全部成功，按序穿过 Wall_15、Wall_20、Wall_22，零碰撞、零越界、
    零超高；因此 r19b 的第三投极限环没有在走廊低空段复现；
  - 新视觉实际发布 `1211` 条 `/detect/land_mark_point`，控制端多次达到连续 10 帧 H 对齐，
    故失败不是 H 未识别；
  - 原逻辑在任一次 H 消息超过 `0.5 s` 后同时清除 stable count 和已完成对齐，使高度目标在
    `land_height=0.30 m` 与 `capture_height=0.75 m` 间往返。bag 中可见 `148.359 s` 开始下降，
    `159.058 s` 到达 0.30 m setpoint，随后因短暂检测间歇又回到 0.75 m，直到 75 秒 LAND
    事务超时仍未完成 AUTO.LAND；
  - 现在仍要求 fresh H、合法 frame/stamp、锚点范围和连续稳定帧才能首次完成对齐；一旦通过，
    将该地图锚点锁存到本次 LAND 事务结束，后续短暂丢帧不撤销、不重新爬升，也不追逐新检测抖动。
- 验证结果：`patrol_control` 编译 PASS，外部任务合同定向 10 项 PASS；`uav_mission` 定向构建
  PASS，包内汇总 `209 tests, 0 errors, 0 failures, 0 skipped`；`git diff --check` PASS。
- 遗留问题：修复后的 post-delivery headless 尚未实跑；r20 因原状态机超时，整轮 Gate 仍为
  FAIL，不能作为自主降落通过证据。
- 下一步：先提交并推送修复，再只复跑一轮相同 seed 11、KS2A543、新视觉的 post-delivery
  headless Gate；PASS 后再按既定安排运行一次 GUI + RViz 复验。

## 2026-09-04｜修正正式仿真 AUTO.LAND 参数类型

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r21_20260904_200913/`
- 改动范围：只修正两份新视觉正式路线配置的 `switch/auto_land` YAML 类型并冻结合同；不改变
  `simulation/enable_auto_land` 默认边界，不修改旧实机/旧仿真配置或 MAVROS 模式切换实现。
- 具体改动：
  - r21 中 H 锚点在 ROS `146.192 s` 连续 10 帧后一次锁存，飞机随后持续下降到约
    `0.33 m`，证明上一提交已消除 0.30/0.75 m 高度往返；
  - `simulation/enable_auto_land=true` 已明确载入，但 `switch/auto_land` 在新视觉配置中写为整数
    `1`；C++ 以 bool 类型读取时 XmlRpc 类型不匹配并静默采用默认 `false`，所以持续报告
    `AUTO.LAND blocked: explicit simulation gate is not enabled`；
  - 将 `patrol_toudi3_new_vision.yaml`、`patrol_toudi4_new_vision.yaml` 的该值改为 YAML 布尔
    `true`，保留只有正式 SITL launch 同时显式打开 `simulation/enable_auto_land` 才能切入
    AUTO.LAND 的双条件；旧 `patrol_real.yaml` 仍保持关闭，不触碰实机默认行为。
- 验证结果：外部任务合同定向 10 项 PASS，`git diff --check` PASS；对正式 post-delivery launch
  执行 `roslaunch --dump-params` 后同时得到 `/simulation/enable_auto_land: true` 与
  `/switch/auto_land: true`。r21 三门按序、H 有效、零碰撞/越界/超高，但因原参数类型仍为 FAIL。
- 遗留问题：参数修正后的 AUTO.LAND、ON_GROUND、disarm 尚未实跑确认。
- 下一步：提交并推送该小修复；不再重复整段路线盲跑，下一次验证应优先使用可复现的降落分段入口，
  若现有入口仍需全路线则只允许再跑一轮并以结果收口。

## 2026-09-04｜撤回 H 直飞专项并恢复整走廊验收

- 日期：2026-09-04
- 分支：`feat/vcl06-local-full-mission`
- 改动范围：按联调决策撤回提交 `daf0227`、`9c4e60a`、`c38568d` 引入的 H 直飞启动、landing
  scope 和相关同包解析改动；保留 `896b642` 的 H 锁存与 `739c453` 的 AUTO.LAND 布尔修复。
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_landing_headless_r23d_20260904_210131/`。
- 判断：r23d 只发送最终 H 点，但从起飞区跨场直飞在 ROS `60.033 s` 后耗尽规划租约，未进入 LAND；
  该路径混入新的跨场规划变量，不能有效隔离或否定降落修复。更早的三次调用均在 launch 解析期退出，
  未解锁、未起飞。
- 验证结果：回滚后 MissionRuntime 18 项、Manager 合同 9 项、Gate/launch 37 项定向回归和
  `git diff --check` PASS；递归 launch 参数确认恢复 `start_mode=post_delivery`、
  `gate_scope=post_delivery`，两个 AUTO.LAND 开关仍均为布尔 `true`。
- 遗留问题：修复后的整走廊、H 对齐、AUTO.LAND、ON_GROUND 与 disarm 仍需一轮有效 headless Gate。
- 下一步：显式指定导航 `UAV_WS`，只运行一次 `navigation_post_delivery_vcl06.launch`；以该轮结果收口，
  不再尝试 H 直飞入口。


## 2026-09-05｜r26 重跑收口失败：过门后偏离撞 Wall_20（新阻塞，走廊段）

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`（工作树含未提交的
  `random_field_spawner`/`coverage_toudi3_random.yaml` 90s 冷加载加固，
  本轮已随实跑生效）
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r26_20260905_112821/`。
- 改动范围：无代码改动；仅补记 09-04 撤回 H 直飞后的第一次
  `navigation_post_delivery_vcl06.launch` 收口尝试。启动方式修正为显式
  `UAV_WS=<本工作树>/patrol_uav_ws-patrol_planner`——不带该变量时
  sim_run.sh:72 会把 pr3 预合并工作树的陈旧 uav_mission 排在 RPP 前列，
  `$(find uav_mission)` 文件级解析被遮蔽（r26 首次尝试 11:04 因此
  `unused args` 解析失败，未起飞；同日 r24 `startup_wall_timeout`、
  r25 `field_status_fail` 与 90s 冷加载加固同源）。
- 运行事实：场地与规划锚点 READY、起飞正常；sim `33.846 s` 即出现
  `current traj in collision` 警告；`129.795 s` 无人机
  `competition_guard_collision` 接触 `toudi2::Wall_20`（一次碰撞即 Gate
  FAIL `actual_collision`）。门洞穿越记录：Wall_15 于 route_index 2、
  z=0.566 m 通过，Wall_20 平面穿越时横向偏移已达 8.204 m；终点
  landing_xy=(3.274, 8.057)，`landing_align_mode_seen=false`——降落链
  本次未能开始。零越界、零高度违规。
- 判断：与 r20（走廊 8/8 干净通过）相比，本轮在门 1 之后即大幅偏离航线并
  撞墙，属于走廊段执行/规划分歧的新发实例，而非降落修复的回归。
  首要怀疑：`planner_anchor_spawner` 锚点 profile 的
  `source_revision=liftrace@4c10f70`（08-29 V-CL-05 随机场同步）与
  a68925d 改造后的 `toudi4_copy.world` 走廊几何不一致，或
  Fast-Planner 近门重规划分歧（r19b 极限环家族）。
- 验证结果：gazebo_contact_monitor 抓到单次碰撞事件（pair/时刻/位置齐
  全）；Gate 结构化输出 failed_checks 17 项（全部为碰撞下游）。
- 遗留问题：走廊路线/锚点与 a68925d 后世界几何的一致性需核对；降落链
  （H 锁存 + AUTO.LAND 布尔修复）仍未获得有效 Gate。
- 下一步：先核对锚点 profile 与当前世界的走廊几何差；在几何对齐前不再
  盲跑整场；30s→90s 冷加载加固连同本条目一并提交。

## 2026-09-05｜补全近目标规划失败风暴的到达收敛窗口

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r26_retry_20260905_120111/`。
- 改动范围：只修正 `PlannerMotionExecutor` 对“已有有效轨迹且已进入目标位置容差圈”场景的
  规划失败次数终态优先级；不修改走廊航点、Fast-Planner 参数、到达位置/速度/驻留阈值、
  任务 deadline、H 对齐或 LAND 实现。
- 问题复盘：
  - `a58b9c1` 已允许有效轨迹之后由里程计完成近目标到达，并在低速 dwell 已开始时延后规划
    失败次数上限；但其豁免条件要求 `dwell_start_ns > 0`，遗漏“位置已到、仍在减速”的窗口；
  - 本轮从 ROS `26.624 s` 开始后段任务，三门按序通过且零碰撞、零越界、零超高；第 8/8
    个 H 前航点于 `127.041 s` 下发，先连续得到 3 次 `TRAJECTORY_READY`，随后飞机进入
    `0.14..0.18 m` 位置误差范围；
  - 因速度尚高于 `0.20 m/s`，低速 dwell 尚未开始；Fast-Planner 在
    `129.951..130.169 s` 的约 `0.22 s` 内密集上报超过 20 次近目标失败，旧逻辑先触发
    `planner_attempt_limit_exceeded`，manager 因 `return_home_failed` 中止，H/LAND 未开始。
- 具体改动：
  - 新增同 frame、新鲜里程计的近目标判定；只有该目标曾产生有效轨迹且最新位置已进入原有
    SEARCH/RETURN_HOME 或 APPROACH 到达半径时，失败次数超限才暂缓为
    `planner_attempt_limit_deferred_for_arrival_settle`；
  - 暂缓期间仍必须满足原速度阈值和驻留时间才能成功，离开位置圈、里程计过期/跨 frame、
    从未产生有效轨迹或总 deadline 到期仍按原逻辑失败；未通过抬高全局失败次数掩盖问题；
  - 增加“位置已到但仍在减速”的复现回归，以及“有效轨迹但仍在位置圈外不得暂缓”的反例；
    保留无有效轨迹必须失败的既有回归。
- 验证结果：`planner_execution` 定向 `27 tests` PASS；`uav_mission` 全包
  `211 tests, 0 failures, 0 errors`；Python 语法、`git diff --check` 与定向 Catkin 构建 PASS。
- 遗留问题：修复后的整走廊尚未实跑；H 对齐、AUTO.LAND、ON_GROUND 与 disarm 仍无最终 PASS。
- 下一步：提交并推送本修复；后续只运行一轮同配置 post-delivery Gate，重点确认第 8 航点可
  等待减速驻留并进入 LAND，不再将同 seed 的重复走廊飞行作为独立业务测试。

## 2026-09-05｜容忍目标切换时规划状态的有界时间戳偏差

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r27_20260905_122544/`。
- 问题复盘：r27 已安全通过前四个走廊航点且零碰撞、零越界、零超高；ROS `102.857 s`
  切换至第 5 个目标时，规划器已用新目标的保留源时间戳正确回显新一代目标，但 roscpp 状态
  时间戳略早于 rospy 回调完成时记录的 `dispatch_ns`，严格的纳秒级先后检查误报
  `planner_event_precedes_dispatch`，任务随即以 `return_home_failed` 中止。
- 具体改动：规划状态仍必须通过目标源时间戳代际解析、requested goal 坐标和 frame 校验；仅将
  “状态早于派发”的检查纳入既有 `source_future_tolerance_ns=100 ms` 有界时钟抖动预算。偏差超过
  预算的事件仍失败关闭，不放宽跨代、错目标或错 frame 事件。
- 验证结果：新增预算边界正反例（100 ms 接受、100 ms + 1 ns 拒绝）；
  `planner_execution` 28 项 PASS，`uav_mission` 全包 212 项 PASS，定向 Catkin 构建与
  `git diff --check` PASS。
- 遗留问题：修复后的 post-delivery Gate 尚待实跑；只有走廊、三门、H 对齐和 LAND 全部 PASS
  后，才启动一轮完整竞赛流程 Gate。
- 下一步：提交并推送同一分支，运行一次 `navigation_post_delivery_vcl06.launch` 收口。

## 2026-09-05｜清理任务运行链的非物理严苛终止条件

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r28_20260905_123754/`。
- 问题复盘：r28 第 1/8 段于 ROS `79.537 s` 成功，第 2 段已有多次有效轨迹并推进至门前
  `(-2.400, 5.908, 0.679)`；`82.975..83.247 s` 的重规划失败风暴中间在 `83.187 s`
  已恢复 `TRAJECTORY_READY`，旧实现仍累计历史失败消息，并在下一次失败时触发
  `planner_attempt_limit_exceeded`。此时该段 60 s deadline 尚余约 56 s；本轮零碰撞、零越界、
  零超高（峰值 `3.604 m`），故这是消息计数造成的伪终态，不是物理不可达。
- 同类盘查：运行链共识别 7 处“通信形式异常直接终止任务”的过严设计，其中
  `planner_event_precedes_dispatch` 与累计失败消息数已由 r27/r28 实证；其余潜在项为 attempt
  必须逐一连续、必须观察到旧目标 CANCELLED、必须观察到 TRAJECTORY_READY、结果时间戳不得
  早于指令任意 1 ns，以及运行中单帧 pose/map 陈旧即 ABORT。
- 具体改动：
  - 删除 `max_planning_attempts` 及其状态计数；`FAILED_ATTEMPT` 只作为进度遥测，真正的规划
    不可达由每段动作 deadline 和 600 s 总时限收口；
  - 允许 attempt 遥测跳号，允许缺失 CANCELLED 后由同一单规划器的新目标 ACCEPTED 完成替换，
    允许 TRAJECTORY_FINISHED 作为权威事实，并忽略结束后的迟到状态；
  - manager 结果时间采用既有 100 ms 时钟容差；任务运行中 pose/map 的短暂 missing/stale/future
    只暂停新指令调度，恢复后仍按绝对 deadline 推进；结构损坏、frame 错误和非有限位姿仍终止；
  - 保留目标代际、requested goal 坐标、frame、有效目标偏移、最大高度、动作 deadline、总时限、
    碰撞以及投递身份/槽位等有物理或业务含义的硬约束。
- 验证结果：新增 50 轮规划失败风暴、attempt 跳号、缺失 CANCELLED、缺失 READY、迟到状态、
  结果时钟容差和运行中短暂输入暂停等回归；`planner_execution` 25 项、`mission_core` 49 项、
  manager 合同 9 项均 PASS，`uav_mission` 全包 210 项 PASS，定向 Catkin 构建 PASS。
- 遗留问题：上述简化尚待一次 post-delivery 实跑；H 对齐、AUTO.LAND、ON_GROUND 和 disarm
  仍必须先获得 PASS，才能启动完整竞赛流程 Gate。
- 下一步：提交并推送同一分支，仅重跑一次 post-delivery Gate；通过后立即运行一次最新导航、
  最新视觉和 KS2A543 新相机的完整竞赛流程 Gate。

## 2026-09-05｜修正窄门规划的末端速度语义与稳定净空锚点

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 问题证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r29_20260905_130435/`。
- 问题复盘：
  - r29 不再触发状态消息计数、顺序或短暂缺帧终止，说明上一提交已关闭非物理合同误杀；
  - 第 2 段目标始终正确保持在 Wall_15 北侧，但 Fast-Planner 在约 45 秒内产生约 70 次
    `current traj in collision` 重规划，发布的 setpoint 从门心 `x≈-2.39 m` 持续向西漂至
    `x≈-3.52 m`，最终真实接触 `toudi2::Wall_6`；碰撞 Gate 判定有效，不予放宽；
  - 2025 走廊配置虽使用门前/门后点，但 `flag_planner_px4=1`，实际由 PX4 直线走点；当前
    Fast-Planner 路径不能只复制坐标而忽略自身的边界条件；
  - Kino FSM 把 `PoseStamped.orientation` 的 yaw 隐式转成固定 0.1 m/s 末端平移速度，而
    Mission Manager 对离散目标使用单位四元数，等价于强制所有路线以 `+x` 速度收尾；Wall_15
    实际要求沿 `+y` 穿越，该隐式合同会弯折窄门轨迹。
- 具体改动：
  - 离散 Pose 目标统一以零末端平移速度规划；机体 yaw 仍由 traj_server 独立处理，不再混同姿态
    与平移速度语义；
  - Wall_15 两侧锚点改为多轮 EDT 实际反复选择后稳定的 `y=5.472270/6.672270 m`，路线版本升为
    `toudi3-random-three-door-h-r3`，避免同一目标在飞行中再被逐栅格外推；
  - 保持三门几何、0.20 m 地图膨胀、真实接触、4 m 高度和 600 秒总时限不变。
- 验证结果：导航 manager 合同 9 项 PASS；`plan_manage + uav_mission` 定向编译 PASS；Catkin
  汇总 `261 tests, 0 errors, 0 failures, 0 skipped`；`git diff --check` PASS。
- 遗留问题：r3 路线尚待一次 post-delivery 实跑确认三门、H、AUTO.LAND、ON_GROUND 与 disarm。
- 下一步：提交并推送同一分支，只运行一次 r30 post-delivery Gate；PASS 后立即运行一次完整竞赛
  流程 Gate，不再用重复走廊仿真代替新增功能验收。

## 2026-09-05｜修复局部轨迹未到目标即结束并恢复实跑航点

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r30_20260905_133414/`。
- 问题复盘：
  - r30 已在门洞中心以 `z=0.812 m` 正向通过 Wall_15，证明 H 之前的第一门识别与 Gate 正常；
  - 第 2/8 航点的局部轨迹在 ROS `110.150 s` 到期时，规划状态记录的实际目标距离仍为
    `0.329 m`，大于任务层 `0.18 m` 到达阈值，也大于规划层 `0.10 m` 收敛阈值；旧 FSM 却
    无条件发布 `local_trajectory_duration_reached`、清除目标并停止重规划；
  - 飞机随后长期追随局部终点 `(-2.304, 6.666, 0.608)`，无法切换 3/8 航点，并在
    ROS `119.008 s` 接触相邻 Wall_16。H 对齐模式尚未启用，故本轮不能解释为 H 识别退化；
  - 2025 比赛配置明确提供 Wall_15 门前/门后点，且以 `flag_planner_px4=1` 直达执行；当前
    r2 是相同物理路线换算到 `camera_init` 后的已实跑版本，不应为掩盖 FSM 提前结束而改点。
- 具体改动：
  - 局部轨迹到期只在目标仍 active、距离有限且进入 `fsm/thresh_no_replan=0.10 m` 时发布
    `TRAJECTORY_FINISHED`；否则保留同一目标并从实测里程计继续重规划；
  - Wall_15 航点恢复 r20/r26 已通过的 r2 坐标 `y=5.672270/6.472270 m`，撤销 r30 前未经
    完整 Gate 证明的各 0.2 m 外推，不改三门、碰撞、高度、任务到达和 600 秒约束；
  - 增加 active、有限距离、阈值边界及终态后的规划完成判定回归。
- 验证结果：`plan_manage + uav_mission` 定向编译 PASS；规划状态测试 5 项 PASS，Catkin 定向
  汇总 `263 tests, 0 errors, 0 failures, 0 skipped`；post-delivery 实跑仍待下一轮收口。
- 遗留问题：三门 8/8、H 对齐、AUTO.LAND、ON_GROUND 与 disarm 仍须实跑收口；未通过前
  不启动完整竞赛流程 Gate。
- 下一步：完成编译与回归后只运行一轮 post-delivery Gate；PASS 后立即进入完整竞赛流程 Gate。

## 2026-09-05｜移除降落阶段无物理依据的零高度下界

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r31c_20260905_140656/`。
- 问题复盘：
  - r31c 已完成 8/8 航段并按序通过 Wall_15、Wall_20、Wall_22，三处穿门高度分别为
    `0.747/0.916/1.100 m`，且零碰撞、零越界、零超高；
  - KS2A543 视觉已进入 landing align，取得锚点误差 `0.0845 m` 的新鲜 H，并于 ROS
    `161.587 s` 启用 SITL `AUTO.LAND`；
  - `camera_init` 局部位姿在下降中于 ROS `162.599 s` 从 `z=0.006 m` 跨至 `-0.012 m`，
    Manager 随即以 `pose_altitude_out_of_bounds` 中止；同轮 Gate 高度违规为 0，说明零高度
    下界误把局部坐标原点当成物理地面，并非竞赛规则或飞行安全约束。
- 具体改动：
  - readiness 继续拒绝非有限位姿和 `z>4.0 m`，但不再拒绝有限负 z；真实降落仍由
    `AUTO.LAND`、MAVROS `ON_GROUND`、disarm、接触与边界 Gate 共同验收；
  - 增加合同回归，固定“4 m 上界保留、camera_init 零高度下界不存在”的语义。
- 验证结果：`uav_mission` 定向回归 PASS，汇总
  `211 tests, 0 errors, 0 failures, 0 skipped`；landing-only Gate 待实跑。
- 遗留问题：本轮在 ON_GROUND/disarm 前被旧判据中止，修复后只需重跑降落专项；不重复走廊。
- 下一步：landing-only PASS 后运行一次最新相机、最新视觉、最新导航的完整竞赛流程 Gate。

## 2026-09-05｜统一外部投递对准截止并冻结实跑走廊航点

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  - `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_post_delivery_headless_r33_20260905_142653/`；
  - `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_full_competition_headless_r34b_20260905_144102/`。
- 问题复盘：
  - r33 已完成 8/8 航段、依次通过 Wall_15/20/22、有效 H 对齐、`AUTO.LAND`、`ON_GROUND`
    与 disarm，且零碰撞、零越界、零超高；走廊与降落链已独立 PASS；
  - 2025 板端 `patrol_real.yaml` 确实提供走廊和降落离散坐标；当前 r2 保持相同的已知门位、
    门前/门后夹点设计，并使用适配随机仿真 world 与 `camera_init` 的坐标。两者 frame 和场地几何
    不同，不能直接混抄数值；r33 实跑结果是当前路线的验收依据，因此不再调整航点；
  - r34b 的第一投 panzer 已获得正向 Servo ACK；第二投 red_cross 于 ROS `175.060 s` 进入对准，
    从 `199.857 s` 起多次满足旧控制的高度与位置投递条件，但释放仲裁直到 `206.364 s` 才取得
    有效几何证据并授权；
  - `CrossDetectionDone()` 的旧 30 s 硬截止已在约 `205.02 s` 关闭执行窗口，而 Mission Manager
    的权威目标事务截止为 `263.242 s`。授权仅晚旧内部截止约 1.34 s，任务仍剩约 56.9 s，故
    r34b 是双重截止冲突，不是走廊航点、目标不可达或视觉未识别。
- 具体改动：
  - 外部任务模式下，红十字对准不再使用旧控制内部 30 s 截止，统一服从 Mission Manager 的
    目标事务 deadline；
  - 旧独立任务模式继续保留历史 30 s 行为，未改变旧链默认语义；
  - 增加单元回归，固定旧模式边界和外部模式单一截止所有权。
- 验证结果：`patrol_control` 定向 Catkin 编译 PASS；包内汇总
  `29 tests, 0 errors, 0 failures, 0 skipped`；`git diff --check` PASS。
- 遗留问题：修复尚待一轮完整竞赛流程 Gate；不得用重复走廊测试替代全链验收。
- 下一步：提交并推送同一分支后，只运行一轮最新 KS2A543、新视觉和新导航的完整竞赛 Gate。

## 2026-09-05｜收口外部任务的重复投递与降落裁决

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 问题证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_full_competition_headless_r35b_20260905_151057/`。
- 问题复盘：
  - r35b 第一目标 panzer 已完成 raw mock ACK、slot 1 commit 和 recovery；第二目标 red_cross
    已进入 `drop_cross` 并在 ROS `271.901 s`、`272.953 s` 等时刻得到释放仲裁许可，但旧
    `CrossDetectionDone()` 仍要求同一控制周期同时满足写死的 `z<=0.17 m` 和三维距离
    `<=0.15 m`。实测许可出现时高度约 `0.19~0.20 m`，因此新仲裁结论被旧局部几何否决，
    最终耗尽 120 s 目标事务；这是双重释放权威，不是视觉未识别或规划不可达；
  - 同类静态盘查发现标准靶、红十字和旧动态靶三条路径均重复实施局部几何与仲裁许可；标准靶
    还保留外部 75 s 对准截止及 70 次本地稳定计数，红十字保留 50 次本地稳定计数，而正式
    `drop_ready` 已在视觉侧完成连续帧判定；
  - 降落控制的 75 s 本地超时早于 Mission Manager 的 90 s LAND deadline，也形成重复终态
    裁决；Gate 的整场 ROS 耗时还错误取同一 LAND 决策最早的 STARTED 事件，而不是终态
    SUCCEEDED 事件。
- 具体改动：
  - 外部任务的释放执行只接受新鲜、激活的 Mission release permission；旧控制的高度/三维距离
    条件仅保留给 legacy 模式，并把 `0.20/0.15/0.10 m` 分别显式配置为旧链高度阈值、距离阈值
    和下降目标，不再散落魔数；外部模式若误配为不要求 permission 则保持 fail-closed；
  - 外部标准靶和红十字均以视觉 `drop_ready` 的既有稳定判定一次锁存下降，不再额外累计
    70/50 个控制回调；legacy 的距离判定、计数和 45/30 s 历史窗口原样保留；
  - 移除外部标准靶 75 s 局部截止，外部目标事务统一由 Mission Manager 的 120 s deadline
    收口；投递后控制交接高度统一为 `0.95 m`，并与 Planner Bridge 配置回归绑定；
  - 降落本地超时改名为 `controller_landing_watchdog_timeout_sec=120 s`，明确晚于 Manager
    的 90 s LAND deadline，只作为控制进程失联兜底；
  - Gate 的 `mission_ros_sec` 只使用 terminal `LANDING/SUCCEEDED` 时间戳，补入同时存在
    LAND STARTED 与 SUCCEEDED 的回归，避免把进入降落误报为完成整场。
- 验证结果：`patrol_control` 定向编译 PASS，34 项测试全过；`uav_mission` 定向编译 PASS，
  211 项测试全过；`git diff --check` PASS。旧链原貌继续由
  `legacy_baseline/20260902_nav_pr7/` 的 27 文件快照、清单与 SHA256 留存。
- 遗留问题：确定性重复裁决已从正式外部任务路径收口，但任何单次改动都不能证明规划器在所有
  地图/seed/负载下零波动；仍需按授权执行且仅执行一轮完整竞赛 Gate，以三投、8 段、三门、
  H、LAND、600 s、碰撞/越界/超高共同判定本基线。
- 下一步：提交并同步同一远程分支；随后运行一次最新 KS2A543、新视觉和新导航的完整竞赛
  headless Gate，归档逐阶段时序与总时长，不启动轻量策略仓探索或整机上机。

## 2026-09-05｜使投递承诺可从瞬态漂移恢复并服从任务代际

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_full_competition_headless_r36_20260905_164607/`。
- 问题复盘：
  - r36 在 ROS `26.000 s` 完成起飞，第一目标 panzer 于 `53.122 s` 完成 slot 1 投递、
    `56.405 s` 完成恢复；相较此前依赖 70 个控制回调的路径，第一投闭环已缩短约 21 s；
  - 第二目标 bridge 于 `148.165 s` 进入 `drop_circle`，`148.356 s` 已取得有效 5 帧视觉证据。
    飞机继续做正常横向修正，于 `164.300 s` 短时超过承诺点 `0.20 m`，旧仲裁器不仅暂停许可，
    还永久清空同一事务承诺；飞机随后回到半径内并最低降至约 `0.115 m`，仍无法恢复；
  - `238.136 s` 再次取得有效证据并锁定同一 bridge/slot 2，但有效窗口内最低高度约
    `0.255 m`，被原 `z<=0.250 m` 精确硬边界再次拒绝，最终在 Manager 的 `253.900 s`
    deadline 到期。全程零碰撞、零越界、零超高，故首个失败仍是投递仲裁假阴性；
  - 固定 45 s commitment timeout 与 Manager 120 s 目标事务并存，会在复杂对准期间形成第二套
    期限口径；同时旧承诺只比较 target ID/slot，未完整冻结 mission/decision/attempt/first_seen，
    存在同一 ID 在下一代事务被误复用的潜在风险。
- 具体改动：
  - 水平漂移仍即时禁止释放，但改为可恢复条件，不再销毁承诺；回到 `0.20 m` 半径后，同一事务
    可继续使用已锁定证据；模式、目标、槽位、结果提交或 deadline 变化仍会终止承诺；
  - 正式 strict-context 路径把 `mission_id + decision_seq + attempt + slot + target_id +
    target_first_seen + class` 固结为代际 fence，承诺绝对截止直接取 Manager decision deadline；
    45 s 仅保留给没有 Manager 上下文的旧兼容入口；
  - 外部投递许可高度上界由 `0.25 m` 调整为 `0.30 m`，覆盖 r36 约 5 cm 的近地周期波动；
    视觉对准、目标身份、证据新鲜度和 `0.20 m` 水平承诺半径均未放宽；
  - 完整 Gate 的 debug bag 新增 permission、permission_active、commitment_evidence、drop_ready
    和 drop_offset，后续无需依靠控制台抽样推断释放拒绝原因。
- 验证结果：新增 r36 漂移恢复、Manager deadline、跨代 fence、上下文结束和投放高度带 5 项
  回归；`patrol_control + uav_mission` 定向编译 PASS；`uav_mission` 汇总
  `216 tests, 0 errors, 0 failures, 0 skipped`；Python 语法检查、launch 静态展开及
  `git diff --check` PASS。
- 遗留问题：修复尚未经过飞行实跑；单 seed 完整 Gate 只能确认功能基线，不能消除 Fast-Planner
  数值波动、视觉偶发丢帧或主机负载等统计风险。
- 下一步：提交并推送同一分支后，只执行一轮 seed 11 完整竞赛 Gate；记录三投、搜索、8 段、
  三门、H、LAND 和总时长。轻量仿真策略探索与整机上机部署只列后续规划，本轮不执行。

## 2026-09-05｜统一运动航段时限并覆盖复杂地图离场

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_full_competition_headless_r37b_20260905_173801/`。
- 问题复盘：
  - r37b 已完成 panzer、red_cross、bridge 三投与恢复 3/3，投递发生于 ROS `53.060/185.881/
    219.681 s`；零碰撞、零越界、零超高，最高 `3.537815 m`；
  - 三投后首个走廊点于 `223.831 s` 下发，第三个 bridge 实体靶位于离场路径旁，Planner 合理地从
    `z=1.04 m` 爬升绕障到约 `2.44 m` 后再下降；固定 60 s 截止时仍在持续收敛，距 planner
    effective goal `0.592 m`，因此 Manager 于 `283.867 s` 以 `safety_motion_timed_out` 中止；
  - 独立 PASS 的 r33 中，同一首航段也使用约 `55.8 s`，原 60 s 只留约 4 s 调度与地图复杂度
    余量。r37b 是过严的统一运动 lease，而非视觉、投递事务、航点坐标、碰撞或近目标终态回归。
- 具体改动：
  - 将 `MissionConfig`、ROS 参数默认、普通 VCL06 runtime 与正式随机场 runtime 的
    `motion_action_timeout` 全部由 `60 s` 统一为 `90 s`，消除默认值/配置两套口径；
  - 不改变 8 个实跑航点、Planner effective-goal 到达阈值、门洞判定、碰撞/边界/4 m 高度 Gate；
    420 s 强制返航、180 s 返航降落预留及 600 s 竞赛硬限继续有效；
  - 合同测试同时固定两份 runtime 和 ROS 默认入口为 90 s，后续单点改动会直接失败。
- 验证结果：`plan_manage + patrol_control + uav_mission` 定向编译 PASS；`uav_mission` 汇总
  `216 tests, 0 errors, 0 failures, 0 skipped`；修改未启动仿真或实机节点。
- 遗留问题：90 s 是基于 r33/r37b 实测收敛包络的功能时限，不代表所有 seed、主机负载或真实地图
  都不会出现 Planner 数值波动；统计鲁棒性仍需后续多 seed/整机测试，不应包装成单轮证明。
- 下一步：提交并同步同一分支后，再执行一轮冻结 seed 11 的完整竞赛 Gate；无论结果均先归档首个
  失败或完整时序，不启动轻量策略仓或整机部署。

## 2026-09-05｜统一走廊执行与 Gate 的到达口径

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_full_competition_headless_r38_20260905_181109/`。
- 问题复盘：
  - r38 已完成 panzer、bridge、red_cross 三投与三次恢复，随后第 1/8 走廊航点完成；进入第 2/8
    航段后于 ROS `298.253 s` 由仿真接触传感器确认防护架接触
    `toudi2::Wall_15::Wall_15_Collision_3`，正式 Gate 以 `actual_collision` 终止；零越界、零超高，
    最高 `3.268463 m`，碰撞判定不予放宽；
  - Bridge 对 SEARCH/RESUME/RETURN_HOME 共用 `0.30 m` 到达半径，而走廊 Gate 的路线到达口径为
    `0.18 m`。第 1 段因此在飞机距 Planner effective goal `0.262 m`、高度仍约 `1.15 m` 时即发布
    `motion_arrival_confirmed`，第 2 段从尚未稳定到达门前的位置开始；r33 成功轮切入同段时误差约
    `0.13 m`。这是执行器比验收器提前推进航段的确定性双口径，不是视觉丢检或 Gate 误报；
  - 静态展开正式 launch 共 35 个节点，不含旧 `target_search_manager`；Mission Manager 是唯一策略
    状态机，Planner Bridge 是唯一 `/fastplanner/goal` 发布者，释放仲裁器是投递许可权威，
    `patrol_control` 仅承担规划轨迹跟随、视觉对准和 LAND 底层执行。历史 manager、固定航线和旧
    几何阈值仍留作兼容源码，但在 `external_mission_mode=true` 的正式入口不取得任务裁决权。
- 具体改动：
  - Bridge 的非 APPROACH 到达位置阈值由 `0.30 m` 收紧并统一为走廊 Gate 已有的 `0.18 m`；速度
    `0.20 m/s` 与 `0.50 s` 连续驻留保持不变，不修改门洞、航点、碰撞、高度或 600 s 竞赛约束；
  - 新增跨配置合同断言，强制 `vcl06_planner_bridge.yaml` 的执行阈值与正式随机场 runtime 的 Gate
    阈值一致，后续任一侧单独改动都会在测试中失败。
- 验证结果：Bridge 合同测试 18 项 PASS；`uav_mission` 定向编译 PASS；包级汇总
  `216 tests, 0 errors, 0 failures, 0 skipped`；正式 launch 静态展开确认导航与视觉包分别解析到
  当前两个 feature 工作树，且未启动任何 ROS/Gazebo/PX4 进程。
- 遗留问题：修改尚待一次完整竞赛流程实跑；单次 PASS 只代表冻结 seed 和当前主机负载下的功能
  基线，不能替代后续多 seed 策略统计或实机外参、机构与资源验收。
- 下一步：在同一分支提交并推送后，仅运行一轮最新 KS2A543、新视觉和新导航的完整竞赛 Gate，
  归档逐阶段 ROS 时序、墙钟时长和全部硬 Gate；轻量仿真策略探索与整机上机部署只写后续规划。

## 2026-09-05｜隔离旧选靶输入并增加随机区高空退出段

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_full_competition_headless_r39_20260905_184607/`。
- 问题复盘：
  - r39 已完成 panzer、bridge、red_cross 三投及三次恢复，释放 ACK 分别位于 ROS
    `54.593/177.908/292.225 s`，全程零碰撞、零越界、零超高，最高 `3.238840 m`；
  - 到达半径统一为 `0.18 m` 后，r38 的提前跳段与 Wall_15 碰撞没有复现。但第三投随机终点到
    Wall_15 南侧低空锚点仍被建模成一个动作：Planner 先绕实体靶爬升到约 `3.23 m`，再下降时
    最接近请求点约 `0.397 m`，90 s 截止时仍未满足到达驻留，Manager 正确以
    `safety_motion_timed_out` 中止；
  - 搜索/投递终点随 seed 和中断顺序变化，而走廊入口是已知固定几何。把远距离水平绕障和
    低空门前精确收敛耦合在一个规划动作中，导致独立 r33 可通过、整场从随机终点却不稳定；
  - 静态权威盘查还发现 `patrol_control` 在 external 模式虽不再拥有选靶策略，旧
    `/yolo_detect`、标准靶/红十字/tank 兼容回调仍可改写内部对准状态。释放仲裁会阻止错投，
    但这种旧输入仍可能让低层控制追逐非当前事务目标并浪费 deadline。
- 具体改动：
  - post-delivery route 更新为 r3：先飞到 Wall_15 南侧锚点正上方 `z=2.2 m`（高于 1.5 m
    隔离墙），再原地下降到 `z=0.9 m`，随后执行原 r2 三门与 H 路线；路线由 8 段变为 9 段，
    三门 Gate 索引同步调整为 `3/6/8`，门洞、到达半径、碰撞、高度和总时限均未放宽；
  - 正式 external 模式显式忽略旧 class、selected_target、waypoint/cross/tank mark 与 cross
    status 的策略输入；唯一目标冻结入口为 Planner Bridge 发布的 `MissionCommand::ALIGN`；
  - `MissionCommand::ALIGN` 按目标类别初始化 circle/cross 对准状态，后续仅允许当前新视觉
    `drop_offset/drop_ready` 做末端修正，释放仍只服从任务事务许可；
  - 不启用 2025 `flag_planner_px4=1` 直达控制，不增加第二个 goal/setpoint 发布者。正式链继续由
    Mission Manager 单一策略、Planner Bridge 单一规划目标、patrol_control 单一 MAVROS
    setpoint 输出组成。
- 验证结果：`patrol_control` 编译 PASS；`patrol_control` 汇总
  `34 tests, 0 errors, 0 failures, 0 skipped`；`uav_mission` 全量 Python 合同回归
  `217 tests, 0 failures`；`git diff --check` PASS。
- 遗留问题：本改动可关闭当前已知的确定性重复权威和随机区退出建模缺口，但不能由静态测试证明
  所有 seed、主机负载、视觉偶发丢帧或实机资源/外参风险归零。仍需且只需一轮冻结 seed 11
  完整 Gate 作为本次功能基线。
- 下一步：提交并同步同一远程分支后，运行一轮最新 KS2A543、新视觉、新导航的完整竞赛 Gate，
  归档起飞、三投、9 段、三门、H、LAND 与总时长；轻量仿真策略仓和整机部署只纳入后续规划。

## 2026-09-05｜r40 复盘与 external 单执行链收敛

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_full_competition_headless_r40_20260905_193031/`。
- 问题复盘：
  - r40 使用导航 `8ab54e2`、视觉 `8e53bd0`、KS2A543 和 seed 11，完成
    panzer/red_cross/bridge 三投及三次恢复；零碰撞、零越界、零超高，最高
    `3.434877 m`；
  - 高空退出段和原地下降段分别用时 `21.442 s` 和 `21.156 s`，证明随机区
    退出已从原 r39 的单段低空收敛中解耦；但原地下降后飞机几乎静止，紧接着向
    Wall_15 门后点规划时，Fast-Planner 向西偏移并在 `90 s` 动作 deadline 内未进门；
  - r33 的 8/8 PASS 日志明确记录 Planner Bridge 向 `/fastplanner/goal` 输出，并非 2025
    `flag_planner_px4=1` 直达控制。因此不新增第二套窄门执行器，而是保留 r33 已验证
    的 Planner 链、门前/门后点和 `0.18 m` 严格到达口径。
- 相同家族盘查：
  - external 模式的 `externalMissionTick()` 同时从位姿回调和 20 Hz 控制定时器驱动，
    旧 `Aligning` 分支还会再执行一次对准/投递函数；虽然单线程 `ros::spin()` 不会数据竞态，
    但会让采样计数、服务尝试和日志取决于位姿发布频率；
  - external 模式仍建立旧 class/mark/status/selected_target 订阅，并保留
    `flag_planner_px4` 运行分支；回调入口虽已拒绝旧数据，但运行图和代码语义仍呈现双链。
- 具体改动：
  - external 末端状态只由 20 Hz `cmdCallback` 驱动，位姿回调只更新状态；旧
    `Aligning` 分支在 external 模式只转发当前对准 setpoint，不再二次调用投递状态机；
  - external 模式不再订阅旧 waypoint/cross/tank mark、class、cross status、selected target 和
    legacy servo complete；目标身份只能由 Mission Manager 经 Planner Bridge 的 `MissionCommand`
    进入低层；
  - external `Run_point` 无条件只转发 Fast-Planner 轨迹，2025 `flag_planner_px4` 仅保留给
    legacy 模式；对准完成后先锁定实测位姿，等待下一个有代际的任务指令，不再短暂
    恢复上一条 APPROACH 轨迹；
  - 保留 r33 已通过的 8 个走廊/H 点，仅将新增高空入口点放在 Wall_15 门前点
    南1 m，使下一段沿 `+y` 下降接近，恢复 r33 通过时的进门方向；路线版本升为
    `toudi3-random-three-door-h-r4`，不改门洞、碰撞、高度或 600 s Gate。
- 验证结果：`uav_mission` Python 合同回归 `218 tests` PASS；`patrol_control` 定向编译
  PASS；其 10 项配置测试、2 项 legacy Servo 回归和 11 项 C++ 投递测试全部 PASS；
  `git diff --check` PASS。
- 遗留问题：本轮改动关闭的是已知双驱动/双订阅语义和 r40 原地下降入门问题；
  不可由静态回归宣称 Fast-Planner 所有负载/seed 波动、真实相机外参、实机执行机构或
  LAND 已根治。
- 下一步：提交并同步同一远程分支后，只运行一轮 seed 11 完整竞赛 Gate，无论
  PASS/FAIL 都归档完整时序并停止重复仿真。轻量策略仓与整机上机部署只记入后续规划，
  本轮不执行。

## 2026-09-05｜r41 整场功能 PASS 并统一比赛计时、LAND 与投递审计口径

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 运行证据：
  `/home/xhj/liftrace-worktrees/pr3-premerge-hygiene/logs/vcl06_full_competition_headless_r41_20260905_202359/`。
- 实跑结果：
  - 飞行代码为导航 `3557215`、视觉 `8e53bd0`、KS2A543、seed 11；panzer、bridge、
    red_cross 三投及恢复 3/3，投后路线 9/9，Wall_15→Wall_20→Wall_22、H 10 帧锁定、
    AUTO.LAND、ON_GROUND、LAND `SUCCEEDED`、Manager `COMPLETE` 和 disarm 全部完成；
  - 零碰撞、零越界、零超高，最高 `3.696886 m`；首个任务决策至 LAND 成功
    `429.875 s`，相对竞赛 600 s 余量 `170.125 s`；
  - LAND 指令至成功 `32.726 s`：等待首个 H `18.874 s`，10 帧锁定完成于
    `20.747 s`，AUTO.LAND 交接于 `27.414 s`，ON_GROUND 于 `31.682 s`；
  - 原 `gate_status.json` 的唯一红项是 `mission_wall_within_limit=false`：慢速 WSL/Gazebo
    主机墙钟 `1761.101 s`，但该值不等于比赛飞行时间；原始其他功能/合同检查全部为 true；
  - 三次释放高度为 `0.291/0.293/0.290 m`，均已由 release_guard 的 `[-0.05,0.30] m`
    权威窗口授权；只读 visual_delivery_audit 仍使用旧 `0.25 m`，因此产生重复裁决假 FAIL。
- 具体改动：
  - 完整 Gate 的 600 s 硬限只检查 ROS/飞行时钟；主机墙钟继续记录并给出实时率，但只由
    独立 2700 s watchdog 判断仿真是否卡死，不再参与竞赛 PASS；
  - 移除 `landing_action_timeout` 配置和 ROS 参数。LAND decision 截止统一为任务开始后
    600 s 全场截止，不再建立 90 s“重捕”业务期限；控制侧 120 s watchdog 只处理执行节点
    失联，不接管任务候选或成功判定；
  - visual_delivery_audit 删除自己的 release 高度上下限，只审计 release_guard 已发布的
    permission、严格上下文证据、目标身份、槽位、raw 调用和 ACK，消除 `0.25/0.30 m`
    两套权威；
  - 更新导航 HANDOFF 与视觉集成报告，记录 r41 全时序、25 个离散坐标上限、所有当前有效
    阈值及降落提速优先级。
- 验证结果：`uav_mission` 全量 Python 回归 `220 tests` PASS，三项修改脚本 `py_compile` PASS，
  正式 launch 只读展开 35 个节点 PASS，`git diff --check` PASS；
  使用修正后的逻辑对 r41 原始 JSON/三次 raw context 离线重判，功能 Gate 为 PASS，投递审计为
  `three_drop_chain_complete`。未启动第二轮仿真或任何实机节点。
- 遗留问题：当前结论是固定 seed 11 的 SITL 功能基线；GUI/RViz 展示、多 seed 统计、新相机
  实机外参、视觉+LIO+规划同机负载、QGC/遥控触发、真实舵机与实机 LAND 均须独立验收。
- 下一步：本轮停止重复仿真。性能优化优先预热第 9 段 H 检测，其次专项评估将仿真
  AUTO.LAND 交接由 0.40 m 提到 0.60～0.65 m；搜索航线优化先在
  `sakelier/liftrace-sim` 做多 seed 策略统计，整机部署只纳入后续规划。
## 2026-09-05｜部署前双仓实跑基线冻结

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 改动范围：新增导航部署前基线与收口交接文档；本提交发生在任何导航删除或接口清理之前。
- 具体改动：
  - 将唯一实际完成整场的组合冻结为视觉 `8e53bd0` + 导航 `3557215`，并记录 seed 11、
    `r2026`、KS2A543、模型、三投/三门/H/LAND 和 `429.875 s` 任务时钟。
  - 区分实际实跑 revision 与后继候选：视觉 `e5fdc68` 尚未包含外参 `f0b1b8b`；导航
    `6c59e17` 是修正 Gate/LAND/审计口径的直接子提交，但该精确组合尚未整场实跑。
  - 记录 r41 完整结构化目录当前在 WSL 工程树和已知归档中不可用，只剩报告和部分 ROS 日志；
    最终候选必须重新形成持久化证据。
  - 划定导航/视觉源码、typed H、legacy bridge 与 worktree 清理责任边界。
- 验证结果：
  - Git 验证 `8e53bd0` 是视觉 closeout 候选祖先，之间仅文档变化；`3557215` 是导航
    `6c59e17` 的直接父提交。
  - 三个导航 worktree 均未发现 bag、Gate report、manifest、run.log 或录屏；仿真进程名单为空。
- 遗留问题：导航 typed H/legacy topic 和视觉源码副本尚未清理；build/devel/cache 尚未删除；
  dirty `vcl06-planner-stop-ack` 必须继续保留。
- 下一步：先提交并推送本基线记录，再执行导航侧最小接口清理、回归和可再生产物删除，并回填
  同一 handoff 的第 7 节。

## 2026-09-05｜正式链 typed H、视觉接口去重与空目录构建修复

- 日期：2026-09-05
- 分支：`feat/vcl06-local-full-mission`
- 改动范围：导航侧正式视觉接口、H/LAND Gate、双仓源码边界和导航工作区构建入口；未启动仿真或实机节点。
- 具体改动：
  - `patrol_control` external mode 改为直接订阅 `/uav_vision/detections_mapped` 的
    `TargetDetectionArray`，只选取 `landing_pad + map_valid + geometry_verified` 中几何置信度
    最高者，再复用既有时间戳、frame、新鲜度、锚点距离和稳定帧降落检查；legacy mode 仍保留
    `/detect/land_mark_point`。
  - 正式 VCL06 launch 传入 `start_legacy_compat:=false`，Gate 和可选 rosbag 直接记录 typed
    detections，不再依赖 `/detect/landing_control` 或 `/detect/land_mark_point`；external mode
    不再 advertise 纯 legacy 视觉控制话题。`/detect/point_class` 暂保留，因为它仍是
    release arbiter/Planner Bridge 的有效控制状态输入，不属于无消费者冗余话题。
  - 从导航仓删除 78 个 tracked `vision_ws/uav_vision` 副本文件；视觉源码和消息定义归视觉仓
    单一维护，导航构建固定为先 source 视觉 devel/install。
  - 修复空目录全量构建暴露的两项上游遗漏：移除 `local_sensing` 已无用途的
    `cmake_modules` 依赖，补回 vendored `cv_bridge/src/CMakeLists.txt`。两者均沿用已验证的
    现有工程实现，不改变算法。
- 验证结果：
  - 删除导航视觉副本并清空原 build/devel 后，source `/home/xhj/liftrace/vision_ws/devel`，
    `catkin_make -DROS_EDITION=ROS1 -j1` 从 0 到 100% PASS；`patrol_control` typed H C++ 已实际编译。
  - `uav_mission` 全量纯 Python 回归 `222 tests` PASS，`git diff --check` PASS。
  - 强制把 `ROS_PACKAGE_PATH` 中 `uav_vision` 指向视觉收口候选 `e5fdc68` 后，正式 launch
    `--dump-params` 解析出 889 行参数 PASS；包含 `/uav_vision/detections_mapped`，不含
    `/detect/landing_control` 或 `/detect/land_mark_point`。
- 遗留问题：本次是编译、合同和 launch 静态验收，不继承 r41 飞行 PASS。视觉最终分支仍需
  合入外参 `f0b1b8b`，随后冻结精确双仓 tip 并按用户当轮授权只跑一次完整 SITL Gate。
- 下一步：提交并推送本轮导航源码收口；删除验证生成的 build/devel/cache，回填最终 handoff。

## 2026-09-06｜导航收口清理结果与视觉接力清单

- 日期：2026-09-06
- 分支：`feat/vcl06-local-full-mission`
- 改动范围：仅回填导航最终 handoff 和本地清理结果；不修改运行代码。
- 具体改动：
  - 记录导航功能收口提交 `be85c42` 已推送，区分 r41 实跑组合与尚未整场复跑的收口候选。
  - 记录三个保留 worktree 的最终状态、约 522 MiB 可再生构建产物释放量，以及 dirty
    `vcl06-planner-stop-ack` 清理前后状态完全一致。
  - 给视觉侧 Codex 留下最终分支/外参整合、视觉 install、`integration_manifest.yaml` 与唯一
    联合 Gate 的明确接力清单。
- 验证结果：导航主 worktree、完整任务 worktree、dirty stop-ack worktree 均保留；完整任务
  worktree 在代码提交后 clean 并与远端同步；未发现或启动 ROS/PX4/Gazebo/实机进程。
- 遗留问题：视觉最终 revision 和双仓最终 Gate 尚未形成；因此当前状态是“导航收口就绪，联合
  Gate 待办”，不能宣称新组合已经继承 r41 PASS。
- 下一步：视觉侧完成收口并回传唯一 revision；用户明确授权后再执行一次最终完整 SITL。

## 2026-09-06｜同步视觉最终候选与双仓运行冻结点

- 日期：2026-09-06
- 分支：`feat/vcl06-local-full-mission`
- 改动范围：仅修正导航最终 handoff 中的视觉接力状态；不修改运行代码，不启动仿真或实机节点。
- 具体改动：
  - 只读拉取视觉远端后，确认视觉 closeout 分支已 clean/pushed 至 `9ed1d46`；唯一运行代码冻结点
    为 `9324745`，其祖先已同时包含 KS2A543/r41 候选 `56f0667` 和外参 `f0b1b8b`。
  - 将双仓最终待测运行组合明确为视觉 `9324745` + 导航 `be85c42`，并继续把两仓后继的纯文档
    tip 与运行代码分开记录，避免把尚未实跑的新组合误称为 r41 PASS。
  - 把视觉侧“合入外参、清理工作树和默认旧话题”从待办改为已完成；剩余联合项收敛为 clean
    install、`integration_manifest.yaml`、单一发布者解析和一次最终完整 SITL Gate。
- 验证结果：
  - `56f0667`、`f0b1b8b`、`9324745` 均为视觉 `9ed1d46` 的祖先，视觉工作树与远端同名分支一致且 clean。
  - 按视觉 `9324745` 源码与导航 `be85c42` 只读展开正式 launch，共 889 行参数，保留 typed
    `/uav_vision/detections_mapped`，不含 `/detect/landing_control`、`/detect/land_mark_point`；
    `e5fdc68..9324745` 的消息定义无差异。
- 遗留问题：精确双仓运行代码组合尚未完成 clean install 联合构建和整场 SITL；r41 的历史
  业务 PASS 不能自动转移到该候选。
- 下一步：两仓不再新增功能改动；先生成最终 install/manifest，再在用户当轮明确授权时只跑一次
  完整竞赛 Gate，PASS 后交由人工完成 main 合并和 tag。

## 2026-09-06｜正式执行图去冗余与并行构建依赖收口

- 日期：2026-09-06
- 分支：`feat/vcl06-local-full-mission`
- 改动范围：导航正式 external mission 节点/话题注册、两项仅调试辅助节点的 launch 开关、
  FAST_LIO 消息生成依赖及合同回归；未改任务策略、航点、阈值、视觉算法或实机接口。
- 具体改动：
  - 根据视觉 `feat/vdeploy-final-closeout-plan@0700c34` 的整机 ROS 拓扑复核结果，
    `patrol_control` 在 external mission 模式不再注册无消费者的 `/control1..3` 发布者和
    `/servo/complete` 订阅者；正式投递仍只走 permission-gated `/Servo` 服务，legacy 模式原接口不变。
  - 为 `waypoint_generator` 和 FreeDOM `static_pointcloud_viz` 增加逐层可配置 launch 开关；
    VCL06 正式 launch 明确关闭二者，通用/legacy launch 默认仍开启，避免改变旧工程入口。
  - 为 `fastlio_mapping` 补齐 `${catkin_EXPORTED_TARGETS}` 及本包导出目标依赖，消除空构建目录
    并行编译时 FAST_LIO 抢在 Livox 消息头生成前编译的竞态。
  - 扩充 external mission 合同测试，锁定 legacy 执行话题门控、正式辅助节点关闭及 FAST_LIO
    构建依赖，防止接口清理回退。
- 验证结果：
  - 视觉 `uav_vision;uav_vision_eval` 空目录构建 PASS；随后导航 29 包 `catkin_make
    -DROS_EDITION=ROS1 -j4` 从空 build/devel 并行构建到 100% PASS，FAST_LIO 消息头竞态未复现。
  - `uav_mission` 全量回归 `224 tests, 0 errors, 0 failures, 0 skipped`；`git diff --check` PASS。
  - 正式 `navigation_search_delivery_vcl06.launch` 只读展开为 32 个节点，不含
    `waypoint_generator`、`static_pointcloud_viz` 或 RViz；未启动 ROS master、PX4、Gazebo 或实机节点。
- 遗留问题：精确双仓新组合尚未完成最终整场实跑；拓扑图也须在完整 ROS 图存活时重新导出，
  静态展开不能替代运行时接口审计或竞赛 Gate。
- 下一步：提交并推送本轮导航收口；按用户本轮明确授权只启动一次 seed 11 完整 headless Gate，
  运行中用 rqt_graph 后端导出一次 Nodes-only 拓扑。无论 Gate PASS/FAIL 均归档并停止，不重复运行。

## 2026-09-06｜r42 唯一整场 Gate 与 Nodes-only 拓扑归档

- 日期：2026-09-06
- 分支：`feat/vcl06-local-full-mission`
- 改动范围：仅将唯一一轮最终候选整场 Gate、运行时接口审计和明确失败边界回填至导航 handoff；
  不修改运行代码、参数、航点或视觉资产。
- 具体改动：
  - 冻结并记录受测组合：视觉 `0700c34`（运行代码 `9324745`）+ 导航 `1928f75` +
    KS2A543 + seed 11 + `r2026`。
  - 记录三投/三恢复成功、投后路线第 1/9 段超时，以及 Fast-Planner 361 次可用重规划仍未形成
    `TRAJECTORY_FINISHED` 的确定性证据，避免把结果误归因给视觉或接口清理。
  - 记录同轮 rqt_graph Nodes-only 拓扑和 13 项接口审计 PASS；完整产物保留在 r42 run 目录。
- 验证结果：
  - `gate_status.json=FAIL`，原因为 `manager_failed -> safety_motion_timed_out`；三次释放 ACK 时间为
    `53.314/140.902/195.070 s`，`visual_delivery_audit.json=PASS`。
  - 零碰撞、零越界、零超高，峰值 `3.614988 m`；未进入三门、H 或 LAND。
  - 拓扑快照为 32 节点、231 话题，接口审计全部 PASS；包装器收尾确认本机仿真进程零残留。
- 遗留问题：r41 仍是唯一完整 PASS 的历史组合；新双仓收口候选尚未取得整场 PASS。当前唯一首阻塞
  是投后路线第 1 段在连续有效重规划下进度不足，需复核 2025 走廊直达执行策略与当前规划跟踪耦合。
- 下一步：本轮按用户要求结束且不重跑。后续另行授权时先做 post-delivery 专项修复/验收，再决定
  是否启动新的完整 Gate；不得据本轮结果合并 main 或打最终 Gate tag。


---

## 2026-09-06：修复旧规划事件误伤后继任务及门点到达语义

### 改动范围

- 导航 uav_mission/planner_execution.py 与定向测试；当前用户已授权整套修复和 SITL 验证。

### 具体改动

- 已结束或交给视觉执行的旧目标事件先分流，不再用旧目标偏移/时效使当前新任务 FAILED；保留活动目标检查和旧 CANCELLED 的观测。
- RETURN_HOME/ABORT 按原任务目标检查实际到达，门点被调整后不能以到达替代点推进路线。

### 验证结果

- planner_execution 定向单测 27 项 PASS，新增旧目标 REPLANNING/READY/CANCELLED 迟到交错及原门点到达回归。
- 本提交尚未启动仿真，后续与 Planner 连续性、今年场地和安装外参组合后专项实跑。

### 遗留问题

- 轨迹进度、重规划初始状态、点云高度边界与今年场景仍待修复；本提交不代表整场 PASS。

### 下一步

- 修复 Planner 连续性并构建，随后运行用户已授权的专项和整场 Gate。


---

## 2026-09-06：统一轨迹进度并准备低空水平避障实跑配置

### 改动范围

- 导航 Fast-Planner 的 plan_manage、plan_env、uav_mission 配置/launch；视觉 uav_vision_eval 的独立场景和机架。
- 旧规划包修改前快照：导航 `legacy_baseline/20260906_planner_progress/`，含文件清单与 SHA256；不参与编译。

### 具体改动

- 轨迹发送、执行 FSM、碰撞前视统一使用实际位置投影到连续前段的进度；取消全曲线搜索后跳末段的行为。
- 重规划使用实际速度；安全且完整的轨迹继续执行；失败优化不替换已有轨迹；优化结果复查膨胀占用与距离。
- 点云建图补齐高度上限，并可将搜索区域内观测障碍沿 z 延伸，约束水平绕障；场地边界外的门区保持三维建图。
- 新 `navigation_horizontal_search_vcl06.launch`：1.4 m 搜索、0.7 m 条带、1.6 m 取景接近、无槽位补偿；适当加快已有渐进下降参数。
- 独立今年世界去掉四个 bigbox，四棵树加 0.3 m 底座，总高约 1.5 m；保留旧世界用于回归。
- 相机 SDF/TF 同为 body z=-0.16 m，MID360 IMU 位于 z=+0.05 m；考虑模型内部传感器偏移后，FAST-LIO 的 MAVROS IMU 到雷达外参为 [0.011,0.02329,0.03588] m。
- 使用原有消息、任务入口和释放链，无新增协调器、ACK 协议或槽位补偿。

### 验证结果

- 主工作区 catkin_make 完整构建通过；plan_manage 的 catkin 测试汇总 16 项、0 错误/失败，planner_execution 27 项通过。
- XmlLoader 离线展开 33 节点，参数覆盖生效，零解析错误；确认两个槽位补偿数组均为零。
- 这是实跑前提交，尚不代表新场景整场 PASS。

### 遗留问题

- 窄门膨胀余量、实际导航稳定性、低空识别覆盖与取景下降仍须 SITL 实跑判定。
- 相机姿态暂沿用垂直下视；真实安装角如有偏差，应修改同一 SDF/TF 配置对。FCU 安装基准按 iris base_link，内部仿真 IMU 偏移单独处理。

### 下一步

- 按当前用户授权先运行 post_delivery 专项，分析首个失败后修复，再做搜索三投整场。


---

## 2026-09-06：修复实跑发现的回弯前视跳变和拟合端点漂移

### 改动范围

- 导航 plan_manage 轨迹选择、参数化与时间分配；沿用已有规划包快照。

### 具体改动

- 前视同时受沿曲线弧长与飞机距离约束，防止整个小回弯落在 0.4 m 球内时指令跳向另一侧。
- 三次 B 样条拟合后固定起始位置/速度/加速度控制点，完整路径固定终点；统一伸缩时间保持曲线几何及端点位置。

### 验证结果

- r44 专项 `logs/vcl06_horizontal_r44_post_20260906_185843/` FAIL：首航段 90 s 超时，零碰撞，包装器收尾零残留。
- bag 回放定位：成功样条起点距规划实际起点约 0.424 m；50–96 s 前视在约 1.4 s 与 4.7 s 两段间切换，导致来回追逐。不是门宽不足或通信适配器失效。
- 增加“整个回弯在球内”的定向回归；构建与 plan_manage 测试通过后提交。
- 相机原图已留在 r44/snapshots，安装位置影响已在 Gazebo 成像生效。

### 遗留问题

- 新修复尚待同场景实跑；首轮未进入三门及 H 阶段。

### 下一步

- 原 seed、原门点、原高度配置重跑 post_delivery 专项，仍使用原 Gate。


---

## 2026-09-06：按护圈位置修正门区垂直余量并保留逐靶取景高度

### 改动范围

- 导航 uav_mission 场景配置；patrol_control 外部 ALIGN 的高度初始化。
- 旧控制快照 `legacy_baseline/20260906_camera_alignment/`，含文件清单与 SHA256。

### 具体改动

- 护圈位于机体上方约 0.2 m，因此顶上障碍应向下膨胀 0.3 m；地面障碍向上膨胀 0.1 m。此前两方向误配，已纠正。
- 第一扇 0.5–1.3 m 竖向开口的两端门点从 0.9 降到 0.75 m，给顶部护圈与高度跟踪留余量；水平坐标、接触实体和 Gate 不变。
- 每次外部 ALIGN 恢复配置的取景高度，避免第一投后旧流程的 1.2 m 恢复值覆盖后续取景；十字分支同步处理。
- 新比赛配置去掉正式链不消费的 progressive_descent 参数块。实际下降仍由 drop_ready 后的原控制流程触发，launch 的位置前视从原 0.2 增至 0.4 m；保留零槽位补偿。
- 调试 bag 增加 Gazebo 模型状态与 LIO 里程计，仅用于核对估计坐标和碰撞真值，不进入任务控制。

### 验证结果

- r45 `logs/vcl06_horizontal_r45_post_20260906_191127/`：首段 18.55 s 完成、第二段 6.67 s 完成，约 47.95 ROS s 接触 Wall_15_Collision_3 上门梁，整轮 FAIL、收尾零残留。
- bag 显示穿门曲线 x 约 [-2.417,-2.362]，靠近门心；先前“曲线向左”的初判不准确，实际飞机被阻后向左偏移。原图、轨迹图与 trajectory_review.json 已归档。
- 实时 ROS 拓扑检查 PASS（32 节点、231 话题），未引入额外桥接链。
- 控制程序构建和相关测试通过后提交；本次门区高度修正尚待实跑。

### 遗留问题

- 仍需核对 Gazebo 真值相对 MAVROS/LIO 的高度差，并完成门区及低空三投整场。

### 下一步

- 保留旧成功路线及旧 Planner 有效实现；重跑专项，再进入联合整场验证。用户已要求推远端、保留分支合入 main，并交付精简整机比赛分支。


---

## 2026-09-06：保留原规划动态包络并按用户要求直接进入整场

### 改动范围

- navigation_horizontal_search_vcl06.launch：搜索动态上限恢复旧成功链的 1.0 m/s；实际位置跟踪继续使用 0.4 m 前视。

### 具体改动

- 取消低空配置额外将 Kinodynamic A* 速度包络压到 0.5 的设置；实际飞行仍低空水平绕障，预算速度按 0.5 m/s。
- 用户明确要求停止继续堆叠 post_delivery 专项，下一轮直接运行全任务 full 入口。

### 验证结果

- r46 `logs/vcl06_horizontal_r46_post_20260906_192454/` 起步大量 NO_PATH 后才生成路径，后已到达第一过渡点；按用户转整场要求主动终止，不能计作专项 PASS。
- 样本显示 MAVROS 高度 1.405、Gazebo body 高度 1.516、LIO 高度 1.862，约 0.11 m 的物理高度基准差说明上门梁余量必须考虑机体与护圈。LIO 高度漂移仍需后续结合真值跟踪。
- 对照旧规划器保留 Kino A*、ESDF 与样条优化；使用旧动态上限和已经成功的门前/门后航点方式，水平路线不新增虚拟门廊限制。
- 参数级修改无需重新编译；主程序及控制程序最近构建/回归通过。

### 遗留问题

- 起点不可达是否完全消除及完整低空闭环仍待本轮实跑。此前九段 post_delivery 是新场景验证入口，不是旧 8/8 或 9/9 原配置复跑。

### 下一步

- 单实例 full 仿真；结果归档，形成 liftrace-visionwork 的整机分支并保留分支合入 main。

## 2026-09-06：修正全场定位源分离与 MID360 仿真接线

- 日期：2026-09-06
- 改动范围：新低空场景定位配置、LIO 外部定位输出、SITL 起飞前参数设置、调试 bag。
- 具体改动：r47 完成三次投递、三次恢复、搜索途中零碰撞，门前 Wall_15 上梁碰撞 FAIL，已零残留收尾。199 秒前 MAVROS 高度约 0.90 m、Gazebo body 约 1.41 m，LIO 同时漂到约 -4.63 m。旧链把 LIO 地图和 GPS/气压计位姿都命名 camera_init，却没有外部定位融合，不能通过继续调低航点解决。
- 新配置使用 MID360 自带 IMU；按实际 ray/IMU 安装关系设置 IMU→雷达外参 [0.011,0.02329,0.00588]，地图体素细化到 0.15 m。LIO 位姿扣除旋转后的 IMU→机体安装平移，经标准 MAVROS vision_pose 输入 PX4；不订阅真值、不增加任务协议。SITL arming 节点在请求 OFFBOARD/解锁前设置外部位置/高度/航向融合，关闭 GPS 和气压计定位融合；参数只在新场景且 /use_sim_time 下生效。原实机入口不启用此节点。
- 验证结果：主工作区 catkin_make 全量编译通过；安装外参平移、非水平姿态旋转、无效四元数三项测试通过。r48 将直接执行整场，当前不宣称定位或整场通过。增加原始雷达、两路 IMU、动态 TF 与外部位姿入 bag，便于区分估计误差和规划问题。
- 遗留问题：新 LIO 闭环是否稳定须实跑确认，不能把历史 r33/r41 或 r47 三投等同于新场景全量 PASS。
- 下一步：整场实跑，核对 LIO/PX4/Gazebo 高度与门框点云，并继续完成集成分支和远端交付。

## 2026-09-06：修复仿真机体遮挡雷达并保留气压计融合

- 日期：2026-09-06
- 改动范围：新机架 MID360 安装、定位融合、物理 Gate。
- 具体改动：r48 全场在初始搜索阶段定位失真、越界 FAIL，已零残留停止。原始点云表明地面回波在机架加载后消失；模型射线原点 z=0.05588 m 仅比 iris 实体碰撞盒顶 z=0.055 m 高 0.88 mm，向下扫描撞自身盒体后被最小量程滤掉。恢复 MID360 0.08 m 支架安装，使 ray z=0.13、IMU z=0.12412，机体→相机 z=-0.16 不变；LIO 输出同步更新 IMU→机体外参。该尺寸是当前仿真机架值，实机仍需按安装测量配置。
- 根据用户补充，实际硬件保留雷达 IMU、飞控 IMU 和气压计。恢复 EKF2_BARO_CTRL=1，LIO 为外部高度参考，气压计继续融合；不是替换为额外测距硬件。新场景 Gate 用独立 Gazebo 实体位姿判物理高度、边界和过门，避免估计漂移掩盖真实超高；真值不接入飞行控制。
- PX4 使用官方 -w 参数把参数存储和飞控日志放到本轮 run/px4，避免某轮调参残留影响后续历史场景。
- 验证结果：Gate 40 项回归通过，含独立真值发现超高的新增测试；r48 的估计高度上限约 1.59 m，却发生实体超高，旧报告高度指标不足，不能当作真实高度合格。
- 遗留问题：LIO 正确地面回波和融合稳定性待 r49 全场验证；r48 导航启动时暂存修复尚未提交，运行代码对应随后 8009a7c，除文件末尾空白外无运行代码变化。
- 下一步：继续整场，检查原始下向回波、实体/估计高度、投递和走廊。

## 2026-09-06：按实机正装和 21 cm 相机外参统一融合建图

- 日期：2026-09-06
- 改动范围：新机架外参、PX4 融合选择、FreeDOM TF。
- 用户确认：MID360 正装在机顶，相机位于雷达 IMU 正下方 21 cm；飞控 IMU 和气压计均保留。
- 具体改动：雷达 IMU 机体 z=0.12412，因此相机光心机体 z=-0.08588；Gazebo 相机传感器 pose 与 ROS optical TF 同步修改，精确满足 0.21 m。r49 已证明抬高雷达后地面静止时恢复约 1347 个地面点，但正装飞行后垂直可观测性不足，LIO 高度仍漂移；机体最终到地面而估计仍显示巡航，主动停止并收尾，不标记 PASS。
- PX4 改为只融合 LIO 水平位置和航向，继续以飞控 IMU/气压计估计高度（EKF2_EV_CTRL=9，HGT_REF=0，BARO_CTRL=1）。FreeDOM 仍使用 LIO 输出的 IMU 坐标点云，但通过与控制/相机同源的融合机体 TF 加安装平移建图，去掉两个不同高度源共用 camera_init 名称的错误。真值只进 Gate。
- 验证结果：参数和 SDF/TF 安装几何检查通过；前一阶段 Gate 40 项测试通过。新高度接线待 r50 整场验证。
- 遗留问题：气压计绝对高度偏差在窄门处仍须以实际点云净空约束处理，不能只依赖固定 z 航点；板端需按真实标定载入安装参数。
- 下一步：继续整场并检查融合建图后的门框与实际飞行高度；完成独立集成构建和远端交付。

## 2026-09-06：在 PX4 估计器启动前加载传感器融合配置

- 日期：2026-09-06
- 改动范围：SITL 启动参数入口、新场景气压计测量噪声。
- 具体改动：本机 PX4 参数元数据明确 EKF2_HGT_REF 需要重启；旧做法在 MAVROS 连通后设置，不能保证已启动 EKF 切换到期望参考。改用原生 px4-rc.params 扩展入口：启动前在 run/px4/profile 生成只含参数的文件，先加载 PX4 原默认参数脚本，再加载场景参数，随后原生 rcS 才启动传感器和 EKF。启动前脚本通过 exec 替换为 PX4，不留下辅助 ROS 节点。
- 移除本轮新增的 arming 节点运行时参数设置逻辑，恢复原起飞辅助节点。气压计插件源代码为 1 Pa RMS（约 0.09 m），而默认 EKF2_BARO_NOISE=3.5 m；仅 SITL 将其配置为 0.15 m，保留余量，实机气压计噪声仍需实际测量。雷达 IMU、飞控 IMU、气压计都保留，不引入额外测距硬件。
- 验证结果：原生默认参数顺序、非法数值/命令拒绝两项测试通过；导航全量 catkin_make 编译通过。r50 正式启动前有一次 launch 文件名笔误，未启动飞行链且已收尾；修正后的 r50 飞行中仍有约 0.8–0.9 m 高度偏差，已分析后停止并确认零残留，不标记 PASS。
- 遗留问题：新的启动时序与噪声配置须在 r51 整场验证，不把参数设置成功当作飞行验收。
- 下一步：整场观察机体/估计高度、实际水平避障、三投和走廊，完成独立集成分支。

## 2026-09-06：外部任务自主降落复用于实机入口

- 改动范围：导航 `patrol_control/src/patrol_control.cpp`；视觉联调记录。
- 具体改动：外部任务完成 H 对齐后的 AUTO.LAND 使用既有 `switch/auto_land` 配置，不再额外要求 `simulation/enable_auto_land`。保留旧固定任务的原分支行为；没有新增任务握手或自动解锁。机载应用可复用同一 H 对齐、飞控 AUTO.LAND 和 MAVROS 落地确认链。
- 验证结果：导航全量 catkin_make（-j1、空白名单）通过，日志 `/tmp/liftrace_r52_build.log`。精简分支的硬件入口只展开 XML 验证：22 个节点，无 Gazebo、PX4 SITL、自动解锁、舵机 mock 节点；未启动任何实机节点。正在运行的 r51 仍是修改前 afad81a 二进制，本修复将在整机分支下一轮整场验证。
- 遗留问题：板端实时链、真实飞控参数和现场 H 降落仍须实机单独验收，不能以笔记本仿真代替。
- 下一步：整机独立编译和联合整场仿真通过后合入 main；保留功能分支并推送。

## 2026-09-06：近目标短连接失败继续避障搜索

- 改动范围：Fast-Planner `path_searching`、`plan_manage`、`plan_env` 测试访问声明；整场调试 bag 话题。
- 具体改动：原 Kinodynamic A* 在起点已位于目标三体素范围时，直接连接被占据格挡住就返回 NO_PATH；从其他节点到此范围则提前返回 NEAR_END。改为连接成功才终止，失败继续正常展开，允许在门前或树旁寻找侧向绕行。搜索阶段与最终轨迹验证一致地拒绝地图范围外点；短连接逐点检查包含确切末端。失败重试复用已有 0.75 s 重规划间隔，避免 100 Hz 重复同一失败。调试 bag 增加 FreeDOM 静态点云及规划膨胀图。
- 验证结果：导航全量编译通过。真实搜索器 + 内存占据图 3 个回归通过：无遮挡近目标、短连接被墙阻挡但可水平绕过、封闭墙不能逃出地图。测试 run：`logs/kino_near_goal_regression_20260906_212439/`，3/3、零错误，收尾零残留；该测试没有飞行模拟或硬件服务。初次无 ROS master 的 gtest 启动因 NodeHandle 等待参数服务而中止，已改成 rostest 包装运行。
- r51 整场记录：`logs/vcl06_horizontal_r51_full_baro_20260906_204726/`，三投及三次恢复完成，实际最高高度 1.7748 m、零碰撞/越界/超高；第 2 个门前航段 90 s 超时 FAIL。近目标连接失败累计 6745 次，未放宽到达阈值或把 FAIL 改成 PASS。高度参考修复有效，但新搜索补丁仍需完整飞行验证。
- 保护快照：改动前 `path_searching` 已保存到 `legacy_baseline/20260906_planner_progress/path_searching/`（含清单与 SHA256）；其他规划包已有同批保护快照。
- 遗留问题：新场景完整三门与 H 降落仍待实跑；板端链和实机另行验收。
- 下一步：将修复导入精简整机分支，从该分支独立编译并继续整场。


### 2026-09-06｜修复绕树轨迹拟合与重复规划停滞

- 改动范围：Fast-Planner 搜路采样、曲线选择、水平搜索配置及离线现场复现工具。
- 具体改动：运动基元检查间隔上限 0.02 s，与最终曲线检查一致；当前场景控制点间距由 0.5 m 改为 0.2 m；优化曲线失败时仅保留通过同一完整碰撞/净空检查的初始拟合曲线。机体膨胀、净空阈值及任务接口不变。
- 验证结果：导航完整编译通过（/tmp/liftrace_dense_search_build.log）。r52 在精简整机分支完成 1 投后搜索超时，已主动停止，收尾零残留。冻结该轮真实地图和停滞位置，原方案连续无法下发；修复后 frozen_search_map_dense_20260906_221040 输出完整终点曲线，planner_replay_result.json=PASS，收尾 PASS。此为 ROS 规划复现，不是走廊或整场通过记录。
- 日志位置：r52 原始记录在 r2026-competition-integrated/logs/r2026_integrated_r52_full_20260906_213503；复现记录在 vdeploy-final-closeout-plan/logs/。测试工具接收地图文件、目标和完整规划参数，保留原始结果。
- 遗留问题：尚不能宣称稳定三投；正装雷达/气压计新场景须继续整场验证，首次冻结复现使用静止速度近似。
- 下一步：同步精简整机与 main 集成分支，运行三投、走廊、H 降落完整链条；通过前不合入 main。


### 2026-09-06｜R53 三投实跑收尾与整机拓扑交付

- 改动范围：验收记录、rqt_graph Nodes only 图及脚本 Git 执行位；无新增飞行算法修改。
- 具体改动：记录 R53 三投/三次恢复 3/3、零碰撞与门前超时 FAIL；保存全量原始拓扑、23 节点阅读版、SVG/DOT/交互 HTML 与结构化 ROS 连接关系。修正由 Windows 侧文件模式跟踪造成的脚本 Git 执行位缺失，便于新机器直接执行启动入口。
- 验证结果：R53 源码 133e512；实体最高 1.753 m，无越界/超高；后段第二航点搜路 47.915 s 后出现大幅绕行，90 s 超时中止；Gate FAIL；完整收尾并复查零残留。图的节点形状已核对均为椭圆，标签保留实际 ROS 话题及发布到订阅方向。
- 日志：r2026-competition-integrated/logs/r2026_integrated_r53_full_20260906_221720；6 个 bag 共 5.55 GB，报告 docs/verification/r53/REPORT.md，拓扑 docs/topology/r53/；大日志经预发布归档提供，不进入 Git。
- 遗留问题：当前只确认本轮三投成功；门前规划阻塞与绕行尚未解决，不能宣称稳定三投或整场通过。main 待验收，未合并。
- 下一步：遵照用户“本轮后不论成败均收尾”的要求，停止后续修复与仿真；推送记录和保留的功能分支，后续开发等待用户新任务。


### 2026-09-07｜R54 走廊入口航点适配（仅一轮验证）

- 改动范围：水平场景 runtime YAML、对应 Gate 航段编号和本轮记录。
- 具体改动：参考旧成功链 3557215 的门洞中心线与前后引导点，适配 1.4 m 搜索高度；在原南侧过渡点先降至 1.05 m，再前进 0.5 m 降至 0.75 m，形成门外低位观察点；保留原门前坐标，门后清空点向北延伸 0.2 m 后再升高。路线 9 段变为 11 段，三门评分索引同步为 5/8/10，几何边界和到达标准不变。
- 验证结果：YAML 解析、三门过平面方向和索引、搜索及任务其他配置一致性检查通过；没有编译项变化。R53 既有地图离线检查显示降高/低位接近区有空间，但门洞附近已被膨胀占据，因此本方案依赖低位靠近后的正常地图更新，不能凭离线检查宣称过门成功。
- 遗留问题：本次只尝试航点引导，不改变规划算法、地图膨胀、超时或通信链；门洞可达性须由本轮实际结果判断。
- 下一步：仅启动一次整场 SITL；无论成败都收尾、记录并推送，不再重跑。


### 2026-09-07｜R54 唯一正式测试启动超时并收口

- 改动范围：本轮结果、任务状态及日志归档记录；无追加算法或参数改动。
- 具体改动：记录已推送的 11 段入口引导方案，标注实际尚未执行，保持 main 待验收。
- 验证结果：正式 launch 静态展开通过；唯一正式 SITL 在 180 s 启动期限内仅推进至 run.log 最后 ROS 时间戳 6.178 s，Gate FAIL/startup_wall_timeout，未解锁、未起飞、无任务决策。统一收尾 PASS，零残留，无第二轮。
- 运行说明：预检查引号错误误起空 Gazebo 后已清理；正式仿真随后仅启动一次。记录见精简整机分支 docs/verification/r54/REPORT.md，完整日志 r2026_integrated_r54_guided_entry_20260907_023929。
- 遗留问题：新航路尚未获得飞行验证，本次无法判断入口方案效果；不得称其通过或宣称稳定三投。
- 下一步：按用户要求收口，推送功能分支和记录，不继续修复、不重跑。


### 2026-09-07｜R54 原配置同轮复跑，三投成功、门前超时并收口

- 改动范围：R54 复跑结果、任务状态、拓扑图和原始日志索引；无新增运行代码/配置改动。
- 具体改动：依用户追加授权，以同一 R54 轮号原配置复跑一次，保留首次启动超时目录；补充前三个引导段及第 4 段失败时间线，记录 rqt_graph Nodes only 拓扑。修正首次 bag 缺失 /clock 被误记为 0.000 s 的摘要，run.log 末个 ROS 时间戳为 6.178 s。
- 验证结果：panzer/bridge/red_cross 三投及恢复 3/3；前三引导段 17.438 ROS 秒内完成；第 4 段等待 36.602 秒出首条轨迹，随后绕行并在 90 秒期限耗尽时 FAIL。碰撞/越界/超高均 0，实体最高 1.712 m，门洞 0，未自主降落。统一收尾 PASS，零残留。
- 记录：整机分支 docs/verification/r54/REPORT.md；复跑 logs/r2026_integrated_r54_guided_entry_20260907_025104；首轮启动失败继续保留，同属 R54。
- 遗留问题：航点调整未解决门前最后约 0.5 m 的可达性与绕行；首次启动超时的硬件根因未确定。本轮三投成功不代表稳定性验收，main 不合入。
- 下一步：按用户要求收口，仅推送记录和归档，不继续修复或重跑。


### 2026-09-07｜R54 门前失败离线根因与走廊模式方案

- 改动范围：R54 诊断文档、结构化分析和示意图；无运行代码、参数或接口修改。
- 具体改动：对照 r41 导航 3557215 与当前 planner 历史；确认门前目标格被门柱膨胀占据，近目标继续扩展后接受距目标 7.32 m 的 horizon 轨迹；记录同步搜索阻塞地图更新、旧轨迹继续执行导致新轨迹起点陈旧。提出复用现有 POST_DELIVERY_ROUTE 的走廊模式。
- 验证结果：读取 R54 既有 bag；地图发布间隔 36.659/33.864 s，而上游点云最大间隔 0.205 s、位姿 0.062 s。保留 0.30 m 护圈包络、5 cm 格的离线局部重建使 0.75 m 门前/门心查询为空闲；不是整条路线或实飞 PASS。
- 遗留问题：走廊精细地图、无进展局部轨迹处理及非阻塞搜索尚未实现/飞行验证；不能直接回退 20 cm 膨胀或宣称复用旧版即可通过。
- 下一步：本次按诊断请求提交并推送报告；未启动仿真。后续实施按报告方案处理，实跑须有新的明确启动授权。

- 诊断报告：https://github.com/Qinling-Melon-Farmers/liftrace-visionwork/blob/feat/r2026-competition-integrated/docs/verification/r54/DIAGNOSIS.md


### 2026-09-07｜核正 r41 垂向膨胀与离线对照

- 改动范围：R54 诊断中的旧参数对比；无运行代码或参数改动。
- 具体改动：按 3557215 的 patrol_planner_sim.xml 核实 r41 向上膨胀 0.20 m、向下 0.10 m，修正此前分析表暂按对称 0.20 m 建模的错误，并更新分析包。
- 验证结果：纯离线同一点云重建中，r41 实际参数下 0.75/0.90 m 门前与门心四个查询均空闲；当前 30 cm 包络、5 cm 格下 0.75 m 两个查询也仍为空闲。没有执行 ROS/planner/SITL。
- 遗留问题：有限格点空闲不等于整段动态穿门通过；旧版完整净空检查、当前护圈/姿态及定位配置的差异仍须结合考虑。
- 下一步：推送更正报告并替换本次刚上传的分析补充包；不改变既有仿真结论。

## 2026-09-07 R55 门洞精度、短目标及搜索阻塞修复

- 改动范围：Fast-Planner 的 path_searching/plan_env、水平绕障参数、离线检查及 R55 说明；导航 launch 复用整机分支的同名 YAML。
- 具体改动：5 cm 有界地图保留 30 cm 水平膨胀；连续体素填充、最新点云；占用目标立即失败，短目标要求完整路径，局部路径要求实际接近目标；短连接优先与 0.25 s 搜索预算。沿用任务状态机及原话题，没有新通信契约。
- 验证结果：精简整机视觉/导航构建通过；7/7 C++ 离线测试通过。R54 冻结点云在粗网格复现阻塞，细网格完整接近/穿门路径通过占用和 0.025 m 净空检查；建图及 ESDF 160 ms。完整说明见 docs/verification/r55/FIX.md。
- 遗留问题：新版本尚未整场实跑；板端地图资源与实时链仍需独立验证，不能继承历史 9/9 为新场景通过。
- 下一步：执行用户授权的单次完整 SITL，无论成败收尾并记录推送；未通过不合入 main。

## 2026-09-07 R55 中断后的记录入口与存储检查修复

- 改动范围：整机仿真包装器、记录器 launch、运行说明；不改飞行算法。
- 具体改动：记录器原订阅 /downward_camera/image_raw/compressed，但 R55 实时 ROS 图中该话题没有发布者，bag 也没有 Image/CompressedImage。改为参数化的真实原始图像话题，并补录检测原始输出及 /fastplanner/goal。整机包装器支持 SIM_LOGS_DIR，WSL 通过 SIM_STORAGE_GUARD_PATH 检查 VHDX 所在宿主盘，默认要求宿主盘和记录目录分别保留 20 GiB；不再只相信 ext4 内部虚拟容量。
- 验证结果：launch XML、bash 语法检查通过；缺少 WSL 宿主盘配置时拒绝、当前 F 盘约 5.5 GiB 时拒绝、充足空间目录检查通过。没有启动 ROS/Gazebo/PX4 或新增飞行。
- 遗留问题：R55 既有画面无法补录，H 无有效检测的视觉根因不能仅靠现有 bag 定论；新录图配置尚未飞行验证。20 GiB 是启动前余量检查，不能替代长时间录制的空间预算。
- 下一步：完成 R55 中断记录和原始数据归档、推送功能分支；本轮不重跑、不标记整场 PASS、不合入 main。

## 2026-09-07 R55 三投三门完成、存储中断与日志恢复收口

- 改动范围：R55 实跑报告、恢复产物、拓扑、任务状态和冗余日志清理记录。
- 具体改动：仅启动一轮完整 SITL；宿主 F 盘耗尽后终止故障 WSL 实例并离线恢复。保留原始中断 bag，在 E 盘副本重建索引；依用户授权清理 47.69 GiB 旧大日志和重复归档，保留当前轮次与历史成功记录。
- 验证结果：3 槽投递、11 航段、3 门完成，碰撞 0；goal 19 首轨迹 0.203 s。原 Gate reducer 离线确认过门顺序且无错误。可恢复记录止于 239.976 ROS s，仍 LAND/OFFBOARD，H mapped 检测 0，未见自主降落；最终 Gate 未写出，整场 INCOMPLETE。仿真资源零残留。
- 记录：docs/verification/r55/REPORT.md、recovery_manifest.json、Nodes only 拓扑及远端 sim/r2026-r55-bounded-corridor 预发布。
- 遗留问题：原压缩图话题无发布者，既有画面缺失；H 漏检根因仍待有效画面复核。磁盘内部删除不等于宿主 VHDX 已缩小。main 尚未合并。
- 下一步：本轮收口，推送代码/文档及归档；不新增仿真，未来另行验证 H 降落。


## 2026-09-07 R56 H 取景几何修正

- 改动范围：水平场景最后 H 航点、外部降落取景高度、离线几何验证及相机缺录复核。
- 具体改动：H 模型外圈直径 1 m，与下移相机在 0.75 m 的取景条件不匹配；最后航点和 capture_height 同步改为 1.60 m。沿用现有 planner、走廊状态机、H 结构/稳定帧和 AUTO.LAND 流程，不放宽检测阈值。
- 验证结果：生产几何函数+当前 YAML 对实际 H 纹理重投影检查 3/3 符合预期，旧高度不检出、新高度检出；配置一致性及 launch XML 检查通过。首段 R55 bag 从启动就没有图像，压缩话题无发布者，不能把缺录全部归因于末段磁盘耗尽。
- 遗留问题：离线图像不包含全部光照/姿态因素；新取景与 R55 收尾后录图修复尚待本轮飞行确认。R55 仍 INCOMPLETE。
- 下一步：在已回收宿主空间、WSL 恢复后，执行用户本次授权的唯一一轮整场，归档完整结果并更新报告。

### 2026-09-07｜R56 按用户要求在 WSL 原生目录同轮重跑与收口

- 改动范围：报告、全部参数、拓扑、环境/接口/来源/路线图、历史阶段状态；视觉只读拓扑脚本去掉旧 worktree 硬编码判据。未再改飞行源码。
- 具体改动：本轮源 f0f7480 保持不变，日志、PX4 工作目录与归档恢复到 WSL 项目 logs，不再写其他盘。报告覆盖 R41—R56、R55 中断/错录图/H 几何、三投与失败根因、全部阈值；35 节点 Nodes only 图来自本轮实时注册。R53 Release 清理保留源码 tag，VHDX 实际回收 68.06 GiB。
- 验证结果：R56 同轮重跑三投/恢复 3/3，前三门外引导段完成，goal 19 在 90.04 s 后运动超时，整场 FAIL；零碰撞、越界、超高，Gazebo 机体原点最大约 1.739 m，H 未执行。原图和 PX4 八项参数读回成功，记录缓冲溢出 0，收尾零残留。三时刻冻结地图证实目标由同一普通膨胀点覆盖，水平绕障列贡献 0。只读接口检查按实际注册重跑；JSON/YAML、文档链接和 git diff 在提交前检查。
- 遗留问题：门前持久地图点观测支持与安全高度窗口仍需处理，H 至落地完整记录、多 seed/板端/标定/扰动接管/机械落点仍需验收；main 不合并失败配置。
- 下一步：提交并推送导航 fork、视觉精简和 main 集成准备分支；上传同轮完整归档、报告和拓扑，保留分支；本轮不再自动仿真。小图表/事实和参数入库，bag/大日志/冻结地图原数据只在 run/Release。

本阶段补充：检查发现此前入库的 H 重投影三张 PNG 与几何 JSON 为零字节；已从现存有效源附件恢复，并验证格式。当前 11 项注册接口检查 PASS；包来源继续由 sim_run 校验。

用户最新修正：失败轮次不全量发布 Release；转为追溯 FreeDOM 持久地图点、修复后再验。当前只提交失败报告和小产物，完整成功后再发布全量记录。

### 2026-09-07｜R56 根因修复：接触代理遮挡与门洞清空尺度

- 改动范围：仿真接触代理 Gazebo 插件及模型、FreeDOM 竞赛参数/输入队列、仿真插件搜索路径；实际巡航/三门/H 航点、碰撞几何和规划膨胀未改。
- 具体改动：接触代理采用 Gazebo 碰撞类别掩码，继续与世界/机体发生物理接触，同时不再挡住激光射线；保持其 z=.20 m、原网格和接触传感器。FreeDOM 空闲体素 .40→.10 m，保留 6 次空闲观测与原连接关系；室内范围 15 m、增强深度上限 12 m，提升近距离深度分辨率。输入队列 100→1，避免旧点云积压。实机与仿真共享竞赛建图 profile，接触代理插件仅用于仿真。
- 验证结果：实际 60 帧雷达数据证实近处自身遮挡；Gazebo 自带 ODE 上，完整网格射线 .254933 m，掩码修正后看到后方 1.45898 m，护圈与墙物理接触仍为 1，PASS。生产 FreeDOM 的离线门洞几何对照中 .40 m 始终保留旧点，.10 m 于第 6 帧清除；门梁有效点保留（最少 59、最终 61）。此为几何/生产代码离线对照，非实际穿门 PASS；接下来构建和新授权整场实跑。
- 遗留问题：新完整闭环仍待实跑。不得仅因单点坐标恒定直接删除地图点；不得关闭接触判据或减少护圈余量。失败轮次不发布全量 Release。
- 下一步：完成构建、记录实跑源码后重跑；通过后发布完整成功记录、同步文档并 --no-ff 合并 main。按仓库规则在导航与 main 集成分支保存原包快照，精简分支不带旧包快照。

本阶段验证补充：视觉与导航完整构建 PASS；完整四边形网格三角化探针 PASS，59/60 帧原始回波与代理求交误差≤1.13 mm。增强深度视场同步为实测对应的 [-7,52]°；重新运行生产 FreeDOM 对照，旧点仍在新尺度第 6 帧清除、门梁点保留。所有源码与证据均在 WSL 内，未发布失败全量 Release。

### 2026-09-07｜R56 根因修复后完整 PASS 与最终交付

- 改动范围：最终报告、全部运行参数、实际相机帧/回放索引、rqt_graph Nodes only、来源与当前文档。飞行源码保持 cc899f2，不再追加仿真。
- 具体改动：归档三投/三恢复、11 航段、三门、H 对齐→AUTO.LAND→ON_GROUND→disarm 的完整结果；统一当前文档为 PASS，并保留所有历史失败/中断原结论。新增成功源标签与 main --no-ff 验收计划，功能分支保留；失败轮次不全量发布。
- 验证结果：运行 Gate 37/37 PASS，182.924 ROS s / 762.126 wall s；碰撞/越界/超高 0，收尾零残留。goal 19 首条 ready 等待 .072 ROS s；机体最大 z≈1.760 m，接触代理最高点约1.982 m，落地中心误差约.162 m。原图4046帧，缓冲溢出0；PX4八项读回成功；注册接口11/11。整机及 main 集成分支独立构建 PASS。
- 遗留问题：一次 SITL 成功不等于多场景/板端/实机验收；仍有多 seed与实时负载、板端ROS10min、安装标定、扰动接管、机械与实际落点五项工作包。
- 下一步：提交推送文档、发布完整成功记录，按 --no-ff 合入 main 并打 annotated tag；保留分支和必要历史小材料，清理已归档的大日志副本。

### 2026-09-07｜R57 toudi4 错列墙与0.85m通口首轮配置

- 改动范围：2026场景走廊、投后航点与截面几何、随机摆放静态排除项、Gate配置与回归；Planner/机架/相机/释放链未改。
- 具体改动：确认现世界派生自导航toudi3_random；移除旧Wall_14/15/16/20/22，复用导航toudi4.world的Wall_14和两块错列短墙。原版净宽约.598/.562m，用户确认先.85m通过再严格.80m；首轮只缩短短墙侵入走廊的一端，使两处实际净宽均.850000m。航点改为6个走廊点+1个H取景点，走廊1.4m，H1.6m。Gate按入口+两处绕墙口配置顺序，不冒称原三框门评分；保留碰撞/超高/时限/落地判据。
- 验证结果：几何检查PASS，全部引导线段距墙最小.425m，保留.30m规划膨胀及.025m净空；原40项Gate回归与新增4项错列场景检查通过。搜索树/H/外部边界保持原模型；整机构建后按用户授权运行首轮。
- 遗留问题：0.85m与严格0.80m实跑尚未完成；虚拟接触代理的实机适用前提保持原说明。
- 下一步：首轮0.85m完整通过后才切到0.80m验收；各轮独立记录并强制收尾，失败不全量发布Release。

### 2026-09-07｜R57 0.85m整场通过后切换严格0.80m

- 改动范围：两块错列墙的侵入长度、对应引导中心线和Gate截面。
- 具体改动：按用户“先0.85，能通过后严格0.8”要求，在0.85m完整PASS并零残留后，将两处通口净宽精确收至0.800000m；航点仍6个走廊点+1个H，走廊1.4m/H1.6m，Planner与机架包络未变。
- 验证结果：0.85m整场Gate 37/37 PASS，记录在 logs/r2026_r57_toudi4_085_20260907_172823；0.80m静态几何和Gate回归通过，中心线最小距墙0.400m。0.85m结果不计作0.80m通过。
- 遗留问题：严格0.80m整场实跑待验证；实机安装与虚拟接触代理适用条件保持原说明。
- 下一步：按当前用户任务执行最终尺寸整场验证，成功或失败均强制收尾，保留两阶段独立记录。

### 2026-09-07｜R57严格0.80m失败分析并按指令收尾

- 改动范围：两轮结果/参数/实际轨迹、严格0.80m失败分析、去年机载航点对照；后续全场bag默认关闭。未修改Planner或进行新仿真。
- 具体改动：保留0.85m的37/37 PASS；严格0.80m三投/三恢复、前4/7投后点和2/3通行截面完成，第5点持续占据导致90s运动超时FAIL。新增可复核的静态点/体素与估计位姿对照；明确十组seed执行数0，main不接收失败配置。
- 验证结果：三快照重现目标占据，普通点贡献2、水平列0，目标在地图内。两点190.623→276.497s保留；首次附近MAVROS相对真值XY约(-.067,-.121)m，LIO约(.011,-.070)m；墙体位姿未变。两轮记录缓冲溢出0，收尾零残留。离线检查后续record_debug默认false，不再默认记录全场bag。
- 遗留问题：严格0.80m尚未通过，水平估计/地图边界与先验引导点一致性仍需处理；未把误差来源全部归结为单一估计器或硬件。
- 下一步：按用户“未通过则分析报告收尾、不再新仿真”停止；推送报告并保留分支。未来只有严格0.80m通过后，才进入十seed验证；届时主要保留TXT/JSON及必要局部信息。

本阶段收尾补充：静态展开确认全场bag记录节点不再默认启动；提取完成后删除两轮20个bag，约18.539GiB，保留TXT/JSON、轨迹CSV及关键图像/局部地图；未压缩VHDX。没有启动额外仿真或十seed，失败配置不合入main。

### 2026-09-07｜R58按实际1.5m走廊与去年引导方式修正

- 改动范围：错列短墙场景、返程航点、紧凑仿真记录器；Planner/定位/膨胀参数未改。
- 具体改动：按用户明确的走廊总宽1.5m校正原约1.0704m模型，两处通口保持0.8m；保留错列短墙、搜索树和随机靶场。前后引导距离从0.45m改0.70m，沿用现有速度<=0.20m/s、0.5s停稳判定，7个投后点，通行1.4m/H1.6m。记录器只写JSONL、CSV、参数和最多3份失败局部地图，无发布/控制服务，无全场bag。
- 验证结果：1.5m总净宽、两处0.8m通口和0.70m距离检查通过；原失败冻结图中原目标占据，沿纵向退远后的点空闲。44项Gate回归通过，记录器实际ROS消息序列化/解码检查通过；整机Catkin构建通过。
- 遗留问题：这是几何和引导可达性修正，不宣称已经根治LIO/飞控所有位置误差；仍需完整实跑及十seed评估。接触包络的实机适用条件保持原说明。
- 下一步：按当前用户授权先跑seed11整场，通过后冻结版本运行seed1~10，只保留文本/JSON/CSV与必要局部信息，成功失败均收尾。

### 2026-09-07｜R58首轮搜索孤点成柱失败与结构支持修复

- 改动范围：Planner的附加水平绕障柱生成、相关参数与测试；原始三维占据/膨胀、轨迹算法、1.5m走廊和0.8m通口未变。
- 具体改动：首轮seed11在第二靶接近失败，(-1.488894,3.877989,.516418)单点生成全高柱覆盖桥靶上空；普通3D膨胀贡献0、水平柱贡献1。新规则仅在0.15m XY邻域内至少3点、竖向跨度至少0.20m时外推整柱；其余点仍保留普通三维碰撞占据，不按目标类别/ID开白名单，不注入真值、不删点。
- 验证结果：真实冻结图662个高度候选中657有结构支持；目标孤点不再生成柱，四树分别仍有169/183/166/139个支持点。生产C++类与冻结输入对照0差异，平均约0.618ms/次（笔记本、662点，不是板端整机性能）。4项C++回归、整机Catkin构建通过。前后引导0.70m以墙中面计、到实体面约0.625m，文注已澄清。
- 遗留问题：修复后完整闭环与十seed待实跑；LIO/飞控误差没有因此被宣称全部消除。
- 下一步：先按当前请求重跑seed11，完整通过后冻结版本进行seed1~10；失败首轮保留真实结果，不发布失败全量Release。

### 2026-09-07｜R58沿用去年机架相对朝向，补齐实际接触记录

- 改动范围：返程目标航向及到点检查、仿真接触记录、紧凑日志和R58报告；机架几何、相机外参、搜索三投和Planner轨迹/膨胀不变。
- 具体改动：去年沿Y飞且yaw=0，当前沿X飞，配置返程yaw=90度；距开阔引导点0.50m内转向，到点增加0.10rad航向条件并与现有位置/速度共同停稳0.50s。已有PoseStamped传四元数，不增飞行话题/桥接。仿真原话题改由整机碰撞形状观察器输出，补齐桨盘/机身接触，移除该机型重复护圈发布器，物理尺寸和响应不变。记录器从master读取完整参数，补规划与飞控位置指令CSV及曲线JSONL。
- 验证结果：任务/运行时/执行/适配相关140项Python回归通过，实际ROS消息序列化通过；整机Catkin构建通过。失败冻结轨迹显示外侧墙更早桨盘贴近/重叠迹象，PX4波动而LIO水平观测仍连续；未把单一几何回放写成全部因果证明。R57的0.85m仍保留37/37历史PASS，严格0.80m仍为失败。
- 遗留问题：新修复尚待完整实跑；先前R58两轮根参数文件为部分缓存，报告已披露。整机接触统计较旧护圈统计完整，历史口径不冒充新口径。小型JSON分析与飞行图作为报告附件纳入git，大日志/ULog留在logs。
- 下一步：先seed11完整通过，再冻结同一版本跑seed1~10；无全场bag、无失败全量Release，成功失败均收尾，未通过不合main。

### 2026-09-07｜R58完整接触统计的起降垫地面语义

- 改动范围：horizontal仿真评测配置，飞行算法不变。
- 具体改动：完整接触观察在0.627s捕获静置机身与landing_h起降垫接触，起飞前Gate FAIL；原始启动失败记录保留。核对模型为1×1×0.005m的平面标记，属于起降支撑地面，配置与ground_plane同类处理，仍统计所有墙体/障碍接触，不改变碰撞几何或动力学。
- 验证结果：已读实际SDF厚度5mm；原Gate正确反映未配置起降地面类别的启动问题，本轮未进入飞行，收尾零残留。修改仅launch参数，XML展开检查通过。
- 遗留/下一步：航向修复还未进入实飞验证；修正后重跑seed11，通过后十seed。

### 2026-09-07｜R58入廊先停在宽处，再对齐窄通口

- 改动范围：一个投后引导点及报告，飞行代码和其余参数不变。
- 具体改动：航向修复整场完成三投/三恢复与90度转向，181.265s在入廊停点发生护圈-外侧长墙接触。原停点y=8.16769距外墙0.40m，实际北向偏出目标0.1023m后机体距墙只余0.2977m。现把第2点移至1.50m通道中线y=7.81769，外墙距离0.75m；先在宽处停止北向运动，再斜向第一短墙前点，门口和其余6点保持。
- 验证结果：原轮FAIL并零残留；碰撞时MAVROS-真值XY为(-.00885,-.00585)m，返回段XY误差P95约.0308m，区别于上次飞控大波动。目标/真实轨迹/飞控指令已完整记录。修改后静态几何与引导线净空检查通过，无新算法或门宽修改。
- 遗留/下一步：窄通口和降落仍待通过；按授权重跑seed11，完整通过后十seed，保留失败原始记录。

### 2026-09-07｜R58恢复去年目标附近选点，避免地图更新后锁死

- 改动范围：Planner已有目标调整逻辑与比赛配置；机架、场地、航点、轨迹碰撞阈值及控制参数不变。
- 具体改动：中线停点版本完成三投和入廊、首通口截面，但原门后目标被更新的长墙地图占据，最终90s超时FAIL、零碰撞。查明比赛profile关闭了旧Planner的目标附近调整。现启用已有能力，选点始终围绕原请求航点，半径0.15m、步长沿用0.05m地图分辨率、保持z；有效点仍空闲时不重新选点，避免随噪声跳动。不再硬编码在膨胀后另需0.20m净空，使用同一Planner碰撞净空0.025m；未降低轨迹净空或放宽0.18m原航点到达要求。
- 验证结果：R57严格0.8冻结图原目标改为(1.623022,7.897277297,1.4)，R58冻结图改为(.587997,8.11769,1.4)，均只移动0.05m、对应占据栅格净空0.05m，20次重复结果相同。3项目标调整、4项轨迹进展、5项目标状态回归通过，整机Catkin构建通过。修改前在导航和main集成来源分支保存plan_manage完整legacy快照；精简分支不复制旧包。
- 遗留问题：冻结图选点通过不等于整场轨迹与实机通过，仍需实跑；缺少可行点时保持原规划失败，不越障、不忽略占据。
- 下一步：同场景seed11整场验证，完整通过后冻结版本执行seed1~10，不录全场bag，保留所有失败与收尾结果。

### 2026-09-07｜按用户要求停止R58并整理成功版本对照和实机方案

- 改动范围：文档、离线事实/源码对照及未加载的低空方案，飞行源码和现有运行配置不变。
- 具体改动：停止继续调试，十seed暂停且实际0组；完整记录最后59ed04b三投后179.495s外墙护圈接触FAIL。对照R56/R57成功源码，区分13次提交中的报告产物、10个飞行源码文件及配置/仿真/测试变化。给出场地旋转/平移统一换算、当前frame参数不完整的边界、碰撞评测与共享实机代码的区别。查明旧0.65→0.15→0.65两组固定航点，保留低空0.65/实际≤0.70目标为未加载方案，不直接照搬未解释的升降动作。
- 验证结果：最后仿真已收尾，进程检查零残留；本次新仿真0、实机操作0。硬件入口只静态展开，22节点无Gazebo/接触/真值/mock/自动解锁；识别出返程航向覆盖参数与仿真不一致。最后返程真值z-估计z P50=.0964m、P95=.1462m、max=.1668m，无法据0.65命令保证实际≤0.70。低空相机短边视野约.56m，当前完整1m H外圈取景不能直接下移。报告链接、YAML及JSON解析检查通过。
- 遗留问题：窄走廊完整通过、真实控高、任意场地旋转贯通、板端实时视觉/导航、机械与接管仍待验收；R58不合入main。未实施停止前提出的净空优先选点。
- 下一步：本轮收口，推送文档并保留分支和成功标签。后续仅在新明确请求下恢复工作或仿真。

### 2026-09-08｜R59单轮低空走廊与H降落准备

- 改动范围：可选H笔画几何、独立走廊专项配置/入口与报告。全场默认搜索/三投配置、机架、世界、相机外参、Planner核心及旧控制器不变。
- 具体改动：确认去年实际检测为黑色轮廓椭圆中心，无H字形/完整环检查；当前保留完整环路径，增加默认关闭的两平行笔画+连接横画几何补充。专项目标0.45m、H取景0.50m、Planner顶界0.55m、指令顶界0.50m、真值Gate≤0.70m；跟随步长0.15m、到点0.12m/0.20m每秒/0.80s，两墙间增加横向对齐点。现有post_delivery模式不生成投递完成证据。
- 验证结果：整机Catkin构建、3项C++几何与44项Gate回归通过；93有真值正例最大中心误差5.49px，7负例拒绝；1张旧成功轮低H实图可检出，25边界输入仅观察。静态launch展开与墙面净空检查通过，尚未启动本轮仿真。
- 遗留问题：低空动态通行及H闭环待唯一一轮实跑；实机和任意现场坐标适配不在本轮。
- 下一步：按用户已有的一轮明确授权运行走廊-landing；结束即收尾，不追加或十seed，不把专项当全场三投PASS。

### 2026-09-08｜R59唯一一轮完成走廊/H对准，修正触垫评测遗漏并收尾

- 改动范围：终点平面降落标记的仿真接触分类、回归和结果报告；飞行算法/控制/相机/世界不再修改。
- 具体结果：仅一轮，源码e633a9b。8/8航点、两处严格0.80m通口完成；通过高度.5687/.4842m，全程真值最高.6701m，越界/超高0。184.8s完成10帧H对准，185.772s确认AUTO.LAND，186.821s机体触landing_h_clone，距H中心约.116m。
- 首个失败：终点与起点同为5mm平面H，但评测漏配clone名称，正常触垫触发Gate FAIL并提前收尾。原始FAIL22/31保留；ULog末尾仍armed且landed=false，不能改判整链PASS或补造disarm。现补齐终点地面分类，墙体接触仍计碰撞。
- 验证结果：补丁后45项Gate回归通过；无第二轮仿真。包装器与最终进程检查零残留。只读截图因ROS已停超时，没有重新启动任何ROS/Gazebo。
- 遗留/下一步：走廊和低空H阶段已有本轮事实，完整landing末端验收仍缺ON_GROUND/disarm；按一轮约定本次收口，十seed暂停，不合main、不发布失败全量Release，保留分支与两个历史成功标签。

### 2026-09-08｜R60全场低空走廊阶段接入与验证准备

- 改动范围：任务参数切换、既有Planner/控制参数缓存读取、独立走廊高度Gate、全场配置及报告；旧包快照保存于来源worktree的legacy_baseline/20260908_r60_phase_profile。
- 具体改动：保留搜索三投；廊外高位到点后原地下降0.40m/到达误差0.05m，随后切换R59低空顶界和跟随参数。H低空笔画路径启用。全走廊真实高度≤0.70m，不放松原碰撞/落地/disarm。
- 验证结果：完整Catkin构建、239项回归、launch展开通过；本轮完整Gate/十seed尚未启动。
- 遗留问题：动态阶段交接及最终落地待实跑，实机仍未验收。
- 下一步：按最新授权先完整Gate，PASS后冻结版本十seed；附R56后修订、飞行记录分析及rqt完整/核心/仅飞行三版图后推送。

### 2026-09-08｜R60完整PASS先导、十seed与联合收口

- 改动范围：验收报告/逐组关键记录、R56后修订说明、当前文档、rqt三版图及离线复现工具；飞行源码冻结9cfb3e5未再修改。
- 具体改动：全场seed11先导完整PASS37/37后，固定同版本依次执行seed1–10；三版图由先导真实ROS注册快照经rqt_graph Nodes only后端生成。清理R54失败/R55中断旧全量Release，小附件及Git报告/标签/分支保留。
- 验证结果：先导269.748 ROS秒、三投/三恢复、9/9投后点、两处严格0.80m通口、H对准、ON_GROUND/disarm，走廊最高0.66333m、落地偏差0.03977m、零碰撞/越界/超高；十seed仅5/7两组完整PASS（2/10），8/10三投、6/10全投后路线，全部0bag/清零进程。构建及239项回归已通过；本次离线工具编译、rqt三SVG及本地报告链接校验通过。
- 遗留问题：2组窄口护圈擦墙，4组H阶段桨盘擦墙，2组投递事务失败；确认外部对齐丢标误退旧(0,0)航点漏洞，另有搜索端点占据/目标可达性及seed6落地估计异常。详见docs/verification/r60_full_matrix/FAILURE_ANALYSIS.md。
- 下一步：本轮收口，不追加仿真，不把2/10合入main；main保留R56历史验收。后续软件修复、板端/机械/现场与实机验收待新任务。日志继续仅留WSL项目logs，未再次压缩VHDX。

### 2026-09-08｜R61实测外参、会议规则、旧回退修复与大场地准备

- 改动范围：外部对齐丢标回退、相机/雷达外参与地面参考、保守机架模型、9.6m场地/出生范围/名义航点、规则校正、静态截图工具。
- 具体改动：最终采用16/21/6cm与50×50×37cm，FC落地高0.22m、IMU0.27m、保守包络55×55×40cm；视觉ground_z及AGL检查一致。起飞H居中贴边，终点H边缘留0.10m。目标出生扩到77.76m²，无旧中央24m²限制。固定门偏移只是开发场景，会议要求随机开口识别仍有缺口。外部丢标保持锁定目标，旧独立模式不变；旧包快照保存在来源分支legacy_baseline/20260908_r61_external_alignment。
- 验证结果：完整构建、240项任务/Gate回归、生产分支丢标回归（旧失败/新通过）、SDF及外参/场地静态校验通过。历史R60先导PASS/十seed2/10不覆盖这些新改动。
- 遗留问题：新模型通用动力学未标定/未全场实跑；随机门开口定位、赛前调整后的缓存/触发、机械及板端全链仍待验收。轻量结果仅几何预筛。
- 下一步：按用户截图请求只启动seed11静态Gazebo展示，截图后清零进程，补齐离线策略及完整报告并推送，不执行飞行/投递、不合main。


### 2026-09-09｜R61静态装配收尾、完整报告及部署资源

- 改动范围：当前README/环境/参数/验收/部署文档、R61布局/规则/策略报告、最终原生截图、仿真模型依赖与打包工具。
- 具体改动：完成seed11地图/机体/侧视三张原图和16/21/6cm实时位姿记录；保留R60实际11布局，另区分新9.6m离线样本；轻量仓比较四组假设条件，未替换在线策略。整机/视觉分支将必要12个外部模型/材质（最大MID360网格约31MiB）作为用户要求的仿真资产例外纳入git，保留来源/许可；运行入口加入包内模型路径。整机分支新增机载/仿真源码打包工具，权重只进入忽略的本地deliverables，不入git。来源AGENTS过时tank/强制桨保条款按会议修正，其余流程要求不改。
- 验证结果：整机构建、240项任务/Gate、轻量仓53项测试通过；最终静态预览CAPTURED、实时安装距离检查通过、收尾零残留；当前world/飞机/五靶共13份SDF及25个资源引用闭合。打包脚本语法检查通过，归档在本次提交后生成并读回检查。
- 遗留问题：没有新飞行PASS。通用动力学未标定；随机门开口感知、赛前调整缓存/启动、板端并发与机械实投仍待验收；硬件旧任务高度和新零点需整体配套，不能只替换单一参数。
- 下一步：生成两份本地包并核查；推送现有feature分支，main保留R56，不发布失败全场Release、不再启动仿真。


### 2026-09-09｜双包生成、读回与解包构建收尾

- 改动范围：BUNDLES/R61报告、bundle_validation.json及本地deliverables索引。
- 具体改动：生成fd147c72版机载与仿真源码包，分别17,473,765/38,927,840 bytes；附各自RKNN/PT权重，权重和压缩包只保留本地。清除本次中文路径修复前的无用半成品包。
- 验证结果：两包1225/1417文件全部读回；无build/devel、机械PWM或旧参考副本；仿真当前资源引用闭合；机载解包后视觉+导航在WSL x86_64完整重编译退出0。未执行硬件、ARM板端或飞行。
- 遗留问题：外部ROS/SDK/PX4/plugin等依赖见使用说明；新几何/随机开口/板端整机和机械带载仍待验收。
- 下一步：推送各现有feature并核对远端HEAD，保留main的历史验收，不新增仿真或失败全场Release。


### 2026-09-09｜R62 seed11完整Gate准备与相机录像

- 改动范围：完整Gate场地边界、可选机载相机视频记录器、对应回归与构建。
- 具体改动：发现完整Gate仍继承旧8m场地边界，按当前world内墙面修正为X[-4.8,4.8]/Y[-0.5,9.1]，保持物理碰撞/限高标准。新增默认关闭的图像订阅MP4记录器，seed11启用10fps，并保存每帧源时间戳CSV；不录bag。没有修改航线、动力学或飞行控制；不启用新安全返航设计，420/180秒现有行为保留。
- 验证结果：241项任务/Gate回归通过；视觉catkin构建通过；独立合成图像录像烟测30帧输入/10帧写入，ffprobe确认1280×720 MPEG4可读。
- 遗留问题：新模型与地图尚无全场PASS，R60未闭环问题仍需本轮观察。
- 下一步：按本次用户授权仅运行seed11；FAIL则分析并停止；PASS后同一版本静默完成seed1–10，再集中分析。只有seed11录机载相机视频，不新增全场bag，不提前启用随机门/树箱或新返航方案。


### 2026-09-09｜R62首轮失败后按新授权修复并重试

- 范围：仿真MID360消息配置、保守包络的刚体归属、自身雷达外壳射线设置、可关闭的任务提前返航预算。
- 事实：首轮run r2026_r62_full_pilot_seed11_20260909_013457在8.726 ROS秒触碰Wall_12，未解锁、0任务决策/0投递，已自动收尾；视频75帧可读。发现新装配MID360误用CustomMsg而SITL LIO等待PointCloud2，系复制模型时遗漏旧type=1。静态展示没有运行该链。未解锁滑移指向新动态支撑/约束问题，不能归为Planner重规划失败。
- 修复：雷达恢复type=1；同尺寸55×55×40cm碰撞体直接归主base_link，消除1g独立支撑刚体并保留总质量；仅自身雷达封闭外壳对激光透明，实体碰撞不删。相机16/21cm和环境几何不变。按用户明确答复，本批次early_return_enabled=false，关闭420秒/返航预留对搜索与选靶的限制，仍保留600秒总限时、动作超时和异常结束。硬件入口默认true。
- 验证：244项任务/Gate回归通过，含超过420仍可搜索/接近且截止600；插件实际构建通过；包络世界坐标范围仍z[-0.22,+0.18]，SITL雷达类型与LIO一致。实际生成24个覆盖航点，X[-4.3,4.3]/Y[0,7.1]，0.7m间距、1.4m AGL，匹配9.6m新场地。
- 遗留：动态效果、遮挡和完整搜索投递仍须重试验证。旧fd147c72仿真包含本次暴露问题，不能作为当前可运行版本；机载包不包含这套Gazebo模型/插件，但仍未取得实机验收。
- 下一步：用户已重新授权修复后从seed11开始；通过才冻结版本静默完成seed1–10，未通过则分析收尾停止，不提前推进后续场景/算法。


### 2026-09-09｜R62两次seed11启动失败收尾

- 范围：R62报告/JSON/场景快照、当前README/ROADMAP与本地包状态提示。
- 结果：初次未解锁滑移撞Wall_12；重新授权后的重试机体停稳，但插件link名称与Gazebo include展开名不匹配，插件加载失败，接触心跳缺失、雷达近点被盲区过滤，180s启动超时。两次均0任务决策/0投递，无seed1–10，视频均可读、进程清零。
- 验证与界限：消息类型修正、244项回归和构建通过；动态闭环未通过，不能视作搜索/投递算法试飞失败或成功。记录确切Gazebo错误，不把缺接触数据写成无碰撞验收。
- 部署影响：Gazebo组件不进入机载硬件入口；原fd147c72仿真包存在回归，当前重试版本也未可用；均未实机验收。关闭提前返航只用于本批次全场仿真配置，硬件默认保留。
- 下一步：本次按失败停止，不再启动仿真；推送源码/失败报告，不发布全量失败Release，不合main。后续首先修正实际展开link引用并验证传感器/接触，再申请新的seed11运行。


### 2026-09-09｜按持续任务运行目标修复展开名并补外围模型

- 用户新目标：持续修复直到seed11能建图、起飞、巡航和投递，不追求整场PASS。外围仅补充简单模型增加点云，不做人群拟真；420秒提前返航保持关闭、600秒总限时保留。
- 改动：插件link引用改为实际展开的iris::base_link和mid360::lidar_link；用gz sdf -p实际展开后验证两处链接和碰撞体均存在。FC IMU使用固定关节，符合刚性安装。场地外新增简单墙/柱/箱体，不改场内树箱、门、seed11靶位；布设器忽略indoor_context整体外壳的圆形足迹近似，实际几何仍参与激光与碰撞。
- 验证：SDF展开引用检查通过、外围17个静态几何link，未模拟人群；原244项任务/Gate回归仍为本批次软件验证，动态运行待本次确认。
- 运行范围：保留原完整Gate的异常检查；另从实际LIO/任务启动、解锁/真值高度、搜索位移、至少一次释放并恢复搜索的记录验证本次运行目标。达到后收尾，不宣称完整竞赛PASS，不自动起seed1–10。
- 遗留/下一步：按当前持续授权进行seed11实跑，若未达运行目标则先分析修复再继续；未完成前不标已验收。原两轮失败报告作为历史记录保留。


### 2026-09-09｜主刚体FC采样与无穿透生成位姿

- 上轮证据：022748运行已恢复20000点有效场景点云、LIO/FreeDOM建图及接触心跳；但PX4静止加速度周期波动、位置发散并拒绝解锁，启动超时。未达起飞/巡航/投递目标，持续修复而非宣告完成。
- 改动：将主base_link参考原点放到FC位置，外形/碰撞姿态平移补偿后保持不变；将人工IMU小刚体的质量和惯量合并进主刚体，Gazebo IMU直接采样该刚体，避免固定关节约束力作为传感器信号。保持原IMU噪声、动力参数与健康检查。spawn_z改为0.225，与0.22m支撑高度+5mm H垫一致，不在初始时穿透垫面。
- 验证：XML结构、总质量/形状位姿静态核对，等待实跑确认惯性信号与位置融合；没有关闭健康检查或改用真值控制。
- 范围：仅仿真模型/生成高度，实机硬件配置不改；seed11场内布设/24航点和600秒限制不改。
- 下一步：按活动目标继续seed11实跑，直至观察到建图、起飞、搜索巡航、投递并恢复搜索。


### 2026-09-09｜垂直起飞固定XY独立于Z限幅

- 上轮进展：024628已验证接地抖动消失、LIO/FreeDOM与接触流正常、飞控位置毫米级且成功解锁。随后起飞初段向南侧贴边墙漂移接触，未达到运行目标。
- 直接代码问题：通用三维距离限幅把固定起飞XY拉向当时已漂移的当前位置，输出只保留很小横向误差，削弱贴边起飞的横向保持。
- 修复：仅Takeoff模式在距离限幅后恢复起飞点XY；Z继续原限幅，搜索/Planner轨迹分支保持原行为。修改前patrol_control完整快照与清单/SHA256保存在main集成与导航来源legacy_baseline/20260909_r62_takeoff_xy。
- 验证：直接抽取生产限幅代码的C++14/UBSan回归通过，验证起飞固定XY及非起飞原0.4m向量限幅；导航实际catkin构建通过。
- 遗留/下一步：新控制行为待本轮seed11观察，继续直到建图/起飞/搜索/投递目标达成，保留600秒限制与健康/接触检查。


### 2026-09-09｜匹配SITL悬停推力初值

- 上轮证据：025758已按固定XY发送起飞目标，但机体长时间贴地；PX4悬停初值0.5，电机差动随姿态积分持续增大，刚离地即侧倾向墙。此前XY修复没有根治此动力初值不匹配。
- 计算：当前装配总质量1.577kg、4电机k=5.84e-6、转速映射omega=100+1000*u，mg平衡对应u约0.7134，取0.713作为MPC_THR_HOVER初值；HTE自适应保留。
- 范围：仅/simulation/px4_parameters的SITL启动参数，不改真实飞控配置，不调整碰撞/健康/限高标准。记录精确计算于本轮artifact/hover_thrust_model.json。
- 验证/下一步：模型静态质量与参数核对完成；继续实际seed11验证离地与任务链，不能仅凭推力计算宣称可飞。


### 2026-09-09｜seed11建图起飞巡航首投运行目标达成

- 运行：r2026_r62_operational_seed11_20260909_030841，飞行源码8bedcc0。地图就绪、正常解锁，ROS22.110s真值AGL超过1m，22.848s任务开始；105.570s panzer第1槽raw_actuator_ack成功且仅一次调用；143.001s确认恢复，发布RESUME继续搜索。
- 验证：7198个接触样本、实际碰撞0；最高FC AGL1.580m；原生相机1197帧可读，源图像时间与编码播放时间分别保留；达到本次目标后主动SIGINT收尾、进程0残留。退出130及三槽audit WAITING如实保留，不改写整场PASS。
- 回归与交付：244项任务/Gate、1项生产起飞限幅、4项实际SDF/模型契约通过；相关实际构建通过。补完整报告、轨迹/高度图、首投证据和源参数。打包器增加版本标签，准备替换已知含回归的旧fd仿真包；权重仍只进入本地包。
- 遗留：未稳定三投、未走廊/降落、未随机矩阵或实机。投后恢复37.431s与高度误差需后续梳理，不扩为本次验收结论。
- 下一步：提交推送现有feature、生成本地更新包并读回；完成活动目标，不启动新仿真、不合main或发布全场PASS Release。


### 2026-09-09｜更新本地R62双包并完成交付

- 从干净提交3ed8cdd7导出机载/仿真包，实际运行代码对应8bedcc0；两者之间无飞行行为改动。
- 机载包19,591,146 bytes/1310文件，仿真包41,048,816 bytes/1504文件，逐文件读回与包内容检查通过，包含首投运行报告；权重/压缩包只在本地deliverables。
- 视频保留原run目录，当前包索引已更新，旧fd包标为历史。没有新仿真/ARM运行/机械动作，不声称完整Gate或实机验收。
- 下一步：推送现有feature并确认远端一致，完成本次“建图、起飞、巡航、投递”目标。


## 2026-09-09：完整 seed11 碰靶收尾及搜索返航计划更新

- 范围：R62完整seed11报告、小型记录、搜索返航计划、ROADMAP；不修改飞行行为。
- 结果：代码aedba01，三投回执成功；ROS485.561秒（任务462.893秒）第三投恢复下沉碰红十字靶，完整Gate FAIL；未进入走廊，未启动seed1–10，未追加重跑。
- 分析：控制恢复交接0.73local早于桥0.95local，随后旧规划的当前位置保持指令接管并随下沉漂移。bridge到达后严格视觉对准等待98.706秒。根因链及未确定的动力/融合贡献已在报告区分。
- 验证：完整观察模式6分支检查、Python/launch解析通过；相机4043帧，ffprobe及完整ffmpeg解码通过，零仿真进程残留。无全场bag，无失败全量release，不合main。
- 遗留与下一步：恢复闭环、旧轨迹接管、捕获耗时待修复；原部署包不标为可放飞验收包。安全返航保持关闭。新的实跑需新的明确授权。
